# AGENTS.md — Console on MangoPi F133-A

## Что это
Обучающий bare-metal Rust-проект для MangoPi (Allwinner D1s/F133-A, RISC-V C906).
Цель: научиться выводить изображение на TFT ILI9488 по SPI, в итоге — NES-эмулятор на этом дисплее.
Ассистент выступает как учитель: объясняет по этапам (GPIO → CCU → SPI → ILI9488 → пиксели → картинка → DMA → NES), только то, что нужно для проекта.

## Железо
- Плата: MangoPi MQ (F133-A / D1s, RISC-V ~1 GHz, 64 MB DDR)
- Дисплей: 3.5" IPS ILI9488, 40-pin модуль, SPI режим (4-wire + D/C)
- Загрузка: FEL по USB через `xfel` → `ddr f133` → `write 0x40000000 bin` → `exec`
- Логический анализатор подключён к PC2 (SPI0-CLK)

### Распиновка SPI0
| F133 | Функция | Назначение | ILI9488 |
|---|---|---|---|
| PC2 | SPI0-CLK (func 2) / GPIO | CLK | LCD_SCL pin 11 |
| PC3 | SPI0-CS0 (func 2) / GPIO | CS | LCD_CS pin 9 |
| PC4 | SPI0-MOSI (func 2) | MOSI | LCD_SDA pin 13 |
| PC5 | SPI0-MISO (func 2) | MISO | LCD_SDO pin 14 |
| PE0 | GPIO out | D/C (0=cmd, 1=data) | LCD_RS pin 10 |
| PE1 | GPIO out | Reset | LCD_RST pin 15 |
| PE2 | UART0-TX (func 6) | Отладка | — |
| PE3 | UART0-RX (func 6) | — | — |

### Кнопки (гребёнка P3, сразу за PE2/PE3)
Каждая кнопка замкнута между своим пином и GND, подтяжка вверх внутри F133
(`Func::Input` + `Pull::Up`). Отпущена = 1, нажата = 0. Внешние резисторы не
нужны, GND у всех кнопок общий — три кнопки это 4 провода.

| F133 | Кнопка NES | Зачем |
|---|---|---|
| PE4 | LEFT | идти назад |
| PE5 | RIGHT | идти вперёд |
| PE6 | A | прыжок |

Минимум для Mario — вообще две (RIGHT + A), LEFT добавлен чтобы можно было
отойти и исправить ошибку. `UP` в SMB не используется, `DOWN` нужен только для
труб, `B` — бег и фаерболы (маленькому Mario без бега даже проще управлять).
START физической кнопки не имеет: жмётся сам на 120-м кадре (`nes/input.rs`).

Пины опрашиваются один раз за кадр в `input::tick_frame()`, а не внутри
`poll()`: `poll()` зовёт `Joystick` в момент strobe, то есть в произвольной
точке кадра, а иногда не зовёт вовсе. Дребезг не гасится — опрос раз в ~30 мс,
дребезг короче 1 мс.

## Документация
- `docs/mangopi/f133_user_manual_v1.0.txt` — основной референс (CCU/PIO/SPI/DMA)
- `docs/mangopi/f133_datasheet_v1.2.pdf` — datasheet
- `docs/display/ili9488/` — datasheet, схема, init-код
- `docs/display/ili9488/BOE3.5IPS-ILI9488.TXT` — init-последовательность
- `docs/display/ili9488/invert the colors.txt` — 0x21 для IPS-инверсии
- `docs/display/ili9488/VCOM--uniformity of glow and burnout.txt` — 0xC5 с 0x4D (VCOM = -0.79688V)
- `docs/Allwinner-SoC/Allwinner D1s-F133 RISC-V/` — PDF'ы по SoC
- `docs/links.txt` — ссылки

## Книга теории
- `books/01-gpio.md` — Глава 1: GPIO (CFG/DAT/DRV/PULL, enum API, мигание PC2)
- `books/02-ccu.md` — Глава 2: CCU (тактирование, reset, включение SPI0)
- `books/03-spi.md` — Глава 3: SPI (протокол, регистры SPI0, отправка байта)
- `books/04-ili9488.md` — Глава 4: ILI9488 (4-wire SPI, IM, init, MADCTL, RGB888, MemoryWrite)
- `books/05-dma.md` — Глава 5: DMAC (дескрипторы, DRQ-порты, SPI0-TX = 22, авто-гейтинг, TF_DRQ_EN)
- `books/06-nes.md` — Глава 6: NES (архитектура 2A03/2C02, iNES, мапперы, runes API, план интеграции)

## Структура проекта
```
src/console/
├── Cargo.toml          # runes = "0.2.5" (чистый, с crates.io)
├── memory.x            # RAM @ 0x40000000, 64M
├── .cargo/config.toml  # target riscv64gc-unknown-none-elf
├── run.sh              # xfel build/write/exec
├── run_dma.sh          # то же + захват UART-лога
├── archive/            # старый код + ROM'ы (не удалять, не в git)
│   ├── nes.rs display.rs dma.rs ili9844.rs main.rs.old
│   └── roms/           # mario.nes, nestest.nes, pac-man.nes
└── src/
    ├── main.rs         # init железа → nes::run()
    ├── uart.rs         # UART0 + UartWriter для println!
    ├── utils.rs        # delay + println! (core::fmt::Write)
    ├── ccu.rs          # регистры CCU (enum Peripheral, PLL_PERI, SPI0_CLK)
    ├── gpio.rs         # generic Pin + enum Port/Func/Pull
    ├── spi.rs          # enum Spi + SpiInfo + prepare_dma
    ├── dma.rs          # DMAC (Descriptor, Dma::Channel0, DRQ-порты)
    ├── display.rs      # ILI9488 (Display, flush_buffer_dma, flush_region_dma)
    ├── fb.rs           # framebuffer 480×320 RGB888 (рамка, заливка)
    └── nes/            # NES поверх крейта runes
        ├── mod.rs      # run() — сборка машины + главный цикл + замер FPS
        ├── cart.rs     # StaticCart (no_std, без Vec) + парсер iNES
        ├── mapper0.rs  # свой Mapper0 (NROM) без UB
        ├── palette.rs  # 64 цвета NES → RGB888
        ├── screen.rs   # FbScreen: ppu::Screen → NES_BUF 256×240 + оверлей FPS
        ├── input.rs    # NoInput (заглушка, кнопок нет)
        └── speaker.rs  # NoAudio (заглушка, звука нет)
```

## Дорожная карта
- [x] Этап 0. Чистый старт: main + UART печатает "hello"
- [x] Этап 1. GPIO: CFG/DAT/DRV/PULL. Мигаем PC2 (видно на анализаторе)
- [x] Этап 2. CCU: тактирование и сброс периферии (включить SPI0, PLL_PERI 600 М / 15 = 20 М SCLK)
- [x] Этап 3. SPI: GCR/TCR/FCR/MBC/MTC/BCC. Отправка байта (анализатор)
- [x] Этап 4. ILI9488: init, D/C, заливка цветом (RGB888, 0x21, 0xC5/0x4D)
- [x] Этап 5. Окно и пиксели: framebuffer 480×320 RGB888, draw_number, fill_rect, set_pixel
- [x] Этап 5.5. Framebuffer в RAM + один flush на кадр
- [x] Этап 6. DMA: DMAC channel 0, DRQ SPI0-TX=22, TF_DRQ_EN, авто-гейтинг, ~5 FPS
- [x] Этап 7. NES: runes на экране, кадр 256×240 по центру ILI9488, замер FPS
- [x] Этап 8. D-cache C906: эмуляция 724 → 27 мс/кадр, 1 → 9 FPS
- [x] Этап 9. Pipeline DMA: эмуляция ушла под передачу, 9 → 13 FPS
- [x] Этап 10. SPI SCLK 20 → 37.5 МГц: 13 → 25 FPS
- [ ] Этап 11. Дальше: либо SCLK 50 МГц, либо оптимизация эмуляции ← текущий

## Замеры (25.07.2026)

`mario.nes` (mapper 0), нативные 256×240 по центру 480×320.
`emu` — эмуляция runes (CPU 6502 + PPU + APU), `fl` — передача 184 320 байт
через DMA→SPI, `wait` — простой CPU в ожидании этой передачи.

| Версия | emu | fl / wait | FPS |
|---|---|---|---|
| baseline (D-cache выключен, SCLK 20 МГц) | 724 мс | fl 73 мс | 1 |
| + D-cache, branch prediction | 27 мс | fl 73 мс | 9 |
| + pipeline DMA | 26 мс | wait 46 мс | 13 |
| + SCLK 37.5 МГц | 26 мс | wait 12 мс | 25 |
| + SCLK 50 МГц | 26 мс | wait 2 мс | **33** |

**33× от начала.** Все шаги легли ровно в расчёт:

- pipeline: время кадра стало `max(emu, fl)` вместо суммы (26 + 46 ≈ 73 = fl)
- SCLK 37.5 МГц: 184320 × 8 / 37.5 МГц = 39.3 мс → 25.4 FPS, замерено 25
- `wait` = fl − emu = 39.3 − 26 ≈ 12 мс, сходится

На 50 МГц `wait` упал до 2 мс — SPI перестал быть боттлнеком, дальше упираемся
в эмуляцию. Дальнейший разгон SPI почти ничего не даёт: кадр = `max(emu, fl)`,
а `emu` = 26 мс. Даже если убрать передачу совсем, будет 38 FPS вместо 33.

`fl=73 мс` совпадает с теорией (184320 × 8 / 20 МГц = 73.7 мс) — значит путь
DMA→SPI идёт на полной скорости железа, и это же подтверждает, что
`CPU_HZ = 1_008_000_000` в `nes/mod.rs` верна.

## Что было не так с кэшем (этап 8, сделано)

Читаем вендорские CSR до и после `cache::enable()`:

```
cache before: mhcr=0x109 mxstatus=0xc0408000 mhint=0x4000
cache after:  mhcr=0x17f mxstatus=0xc0608000 mhint=0x610c
```

`MHCR` до = `0x109` — биты 0 (IE, I-cache), 3 (WB), 8 (WBR). То есть
**I-cache был уже включён** (его поднимает ddr-payload самого xfel), а вот
**бит 1 (DE, D-cache) был сброшен — D-cache не работал**. Тормозили не
выборки инструкций, а доступы к данным: PPU и CPU 6502 гоняли каждое
чтение/запись прямо в DDR2-528.

После: `0x17f` — добавились DE (D-cache), WA (write allocate), RS (return
stack), BPE (branch prediction), BTB. Биты 7 и 12 из записанного `0x11ff`
на этом ядре не реализованы и читаются нулями — это нормально.

`MXSTATUS`: бит 22 (THEADISAEE, расширения T-Head) был уже выставлен,
добавили бит 21 (MM — аппаратная обработка невыровненных доступов).

Реализация — `src/cache.rs`. Когерентность с DMA сделана в `dma.rs::start()`:
перед запуском канала зовём `cache::clean_dcache()` (`dcache.call` + `sync.s`),
иначе DMAC прочитает из DDR устаревший дескриптор и прошлый кадр. Один
`dcache.call` чистит кэш целиком, поэтому покрывает и дескриптор, и буфер.

## Как сделан pipeline (этап 9, сделано)

`screen.rs` — два буфера кадра по 184 КБ. `FbScreen` держит указатель на тот,
куда PPU пишет сейчас. `render()` (PPU зовёт его на scanline 241, когда все
240 видимых строк уже в буфере) публикует законченный буфер в `READY_IDX` и
переключает запись на другой. Главный цикл в `mod.rs`: дождаться прошлого
переноса → нанести оверлей FPS на готовый буфер → запустить неблокирующий
`flush_region_dma_start` → вернуться к эмуляции.

Порядок важен: ждать прошлый DMA надо **до** того, как трогаем SPI, потому что
`start()` держит CS низким и сам шлёт команды — две передачи разом невозможны.

Запас по времени большой: между `render()` и ожиданием в главном цикле проходит
одна инструкция 6502, а PPU до первого пикселя нового кадра стоит весь vblank
(21 scanline ≈ 2400 такт CPU). Так что буфер под DMA точно никто не перезапишет.

Отдельно пришлось учесть, что `put()` вызывается только при включённом
рендеринге (`runes/ppu.rs:590`). Mario гасит экран на сменах уровня, и тогда за
кадр не приходит ни одного пикселя — при наивной двойной буферизации экран
мигал бы между двумя старыми кадрами. Поэтому в `FbScreen` есть флаг `dirty`:
нет пикселей → буфер не публикуем и не флашим совсем (заодно экономим 73 мс).

## Про разгон SPI (этап 10, сделано)

Такт задаётся в `ccu::set_spi0_clock_pllperi(m)`: модуль = 600 / m, дальше
контроллер делит на 2 (`spi.rs`, `CLK_CTL`: DRS=1 + CDR2=0). Сейчас `m = 6`
→ модуль 100 МГц → SCLK 50 МГц. Таблица значений — в докстринге функции.

Замерено на железе: 37.5 МГц → 25 FPS (`wait` 12 мс), 50 МГц → **33 FPS**
(`wait` 2 мс). То есть на 50 МГц SPI перестал быть боттлнеком, дальше упираемся
в эмуляцию.

**Это разгон.** Datasheet ILI9488 даёт минимум 50 нс на цикл записи, то есть
штатный предел — 20 МГц; 50 МГц это ×2.5. На конкретном модуле работает, но это
не гарантия: отказ проявится не честной ошибкой, а битыми пикселями или сбитой
синхронизацией. Если появятся артефакты — снижать `m` (8 → 37.5 МГц, 15 → 20).

Дальше по SPI смысла почти нет: `m = 5` даёт 60 МГц → fl 24.6 мс, но кадр всё
равно ограничен `emu` 26 мс, то есть **потолок SPI-пути ≈ 38 FPS**.

## Что НЕ дало ускорения (проверено на железе)

Чтобы не переделывать по второму разу:

- **`opt-level` 2 → 3**: `emu` 26 мс без изменений.
- **Убрать `write_volatile` из `FbScreen::put`** (3 записи на пиксель → обычные
  записи): `emu` 26 мс без изменений. Компилятор и так справлялся, а путь
  записи пикселей — не боттлнек. Изменение оставлено (так правильнее: буфер
  это RAM, а не регистры, и видимость для DMA даёт `clean_dcache()`), но
  выигрыша не ждать.

Оба замера делались при `fl` 39 мс, то есть FPS всё равно был прибит SPI —
но `emu` мерится независимо и не двинулся.

## Ablation: сколько стоит вывод пикселей (замерено)

Замер разовый, код после него убран (сама проверка режима стоила ~1 мс на кадр).
Схема была такая: статик-переключатель режима `put()`, переключается из главного
цикла каждые 50 кадров — так все четыре режима меряются в одной прошивке, а
перепрошивка требует ручного reset платы в FEL и четыре прогона дороже одного.
Разброс внутри режима ±30 µs, так что разница в 150 µs уже значима.

| режим | что делает `put()` | emu | instr/кадр |
|-------|--------------------|---------|------------|
| 0 | палитра + 3 записи | 27 100 µs | 19 802k |
| 1 | без палитры, 3 записи | 26 920 µs | 19 618k |
| 2 | палитра + 1 запись | 27 140 µs | 19 802k |
| 3 | ничего | **25 670 µs** | **19 250k** |

Выводы:

- **Всё тело `put()` — 1.4 мс из 27, то есть 5%.** Оптимизировать путь записи
  пикселей бессмысленно, там нечего забирать.
- `palette::rgb` — 0.18 мс (3 инструкции на пиксель). Таблица 256 байт лежит
  в кэше.
- **Одна запись на пиксель стоит столько же, сколько три** (27 140 против
  27 100 — в пределах шума). При write-allocate платим за заполнение кэш-линии
  из DRAM, а не за байты: при шаге 3 байта одна запись трогает те же 2880 линий
  на кадр, что и три. Значит **переход на RGB565 не ускорит эмуляцию** — только
  сократит SPI-трафик (а по SPI ILI9488 16 bpp и не умеет).
- Тело `put()` — 552k инструкций (9 на пиксель) при 1.44M такта, то есть 2.6
  такта на инструкцию: это и есть промахи на записи. Остальной код идёт с 0.71.

## IPC: природа 26 мс (замерено)

`instrs()` в `nes/mod.rs` читает `minstret` (CSR 0xb02) в той же области, что и
`emu`. Результат: **19.8 млн инструкций на кадр при IPC 0.71**.

| на единицу работы NES | инструкций хоста |
|---|---|
| точка PPU (89 342 за кадр) | **222** |
| такт CPU (29 780 за кадр) | 665 |
| инструкция 6502 (~9 300 за кадр) | ~2 130 |

Это решает выбор направления:

- **Ядро не стоит на промахах кэша.** 0.71 для single-issue in-order C906 —
  нормально. Работа с раскладкой данных даёт максимум 1.4× (до IPC 1.0), а
  нужно 1.56×.
- **Надо резать количество инструкций.** Бюджет на 60 FPS при том же IPC —
  12.0 млн против сегодняшних 19.8, то есть минус 40%.
- 222 инструкции на точку PPU — это в разы больше, чем стоит сама работа
  (несколько сдвигов и обращений к таблицам). Хорошие эмуляторы NES тратят
  50-100 инструкций на **такт CPU**, у нас 665.

19.25 млн из 19.8 — внутри runes (режим ablation 3).

Оценка для оставшегося пункта: `&mut dyn` в горячем пути стоит ~1-2 мс из 26.
С включёнными кэшами косвенный вызов дёшев (vtable лежит в D-cache), так что
дженерики вместо dyn — это единицы процентов, а не разы. Плюс требует
вендорить runes локально, чтобы его патчить.

## Про параллельный интерфейс (изучено, не сделано)

Вопрос был: дисплей подключён проводами по SPI — нельзя ли воткнуть его в
дисплейный разъём платы?

**Напрямую — нет.** У MangoPi MQ есть разъём `FFC40-RGBLCD` (схема
`docs/mangopi/mq_sch_v1.6.pdf`) и `Header15-MIPI`. FFC40 — это параллельный
**RGB666**: R0-R7 / G0-G7 / B0-B7 (реально задействованы D2-D7, D10-D15,
D18-D23 на PD0..PD21), плюс DCLK, DE, HSYNC, VSYNC, DISP, резистивный тач и
подсветка. Он рассчитан на «глупую» матрицу без контроллера и без GRAM.

У нашего модуля (`docs/display/ili9488/`, спека `3.5寸TN_规格书_ILI9488_40PIN`)
на 40-пиновом шлейфе совсем другие сигналы: CS, RS, WR, RD, RESET, DB0-DB15,
IM0-IM2, FMARK. Интерфейсы модуля — `8080 3/4WIRE-SPI, 8/16bit`, RGB-режим на
шлейф не выведен. 40 контактов с обеих сторон, но electrically несовместимы —
воткнуть значит подать выходы PD на DB и упереть выход в выход.

**Но параллельный путь существует, только через 8080, а не RGB.** F133 TCON
умеет i8080 нативно (мануал 5.1.3.5, «Supports i8080 interface, up to
800x480@60fps»): `LCD_CTL_REG[LCD_IF] = 01`, режим в `LCD_CPU_IF_REG[31:28]`
(есть 16-bit mode0..3, 18-bit, 8/9-bit), сигналы CS / WR / RD / A1 / D[23:0]
(таблица 5-7), причём «WR is 180° delay of DCLK», а в AUTO-режиме кадр гонит
Display Engine прямо из DRAM — CPU не участвует вообще.

Что это даёт: 16 бит за один цикл WR вместо 24 бит последовательно. По IC spec
цикл записи 50 нс → 61440 пикселей × 50 нс ≈ **3 мс на кадр** против 29.5 мс
по SPI, и без участия CPU. Плюс RGB565 (1 запись на пиксель) — по SPI ILI9488
16 bpp не поддерживает, там всегда 3 байта.

Чего это стоит:
- поднять DE + TCON bare-metal (это не пара регистров; плюс открытый вопрос из
  `docs/links.txt` — что именно включает доступ CPU к 0x05xx);
- перестрапить IM0-IM2 на модуле (0R-резисторы на плате модуля) с SPI на
  16-bit 8080, и убедиться, что DB0-DB15 вообще выведены на гребёнку;
- 19 проводов вместо 6 (DB0-DB15 + WR + CS + A1);
- уточнить, на какие физические пины TCON выводит CS/WR/RD/A1 (в мануале
  таблица 5-7 описывает сигналы, но не привязку к PD-пинам — смотреть
  `drivers/gpu/drm/sun4i` в Linux).

**Сейчас это не даст почти ничего.** На 50 МГц `wait` = 2 мс, то есть flush уже
спрятан за эмуляцией. Убрать flush совсем → кадр = `emu` 26 мс → 38 FPS, ровно
столько же дал бы SPI на 60 МГц. Параллельный интерфейс становится осмысленным
только после того, как `emu` опустится ниже ~16 мс.

## Этап 11: что осталось

Боттлнек — **эмуляция**: `emu` 27 мс, `wait` 2 мс. Цель — 16.6 мс (реальная
скорость NTSC NES, 60.0988 кадра/с). Сейчас игра идёт на 55% от настоящей
скорости, ограничителя кадров нет, так что скорость прямо пропорциональна FPS.

Всё, что снаружи runes, уже измерено и там ничего нет (см. две секции выше).
Осталось единственное направление: **сократить 19.25 млн инструкций внутри
runes на ~40%**. Следующий шаг — не переписывать наугад, а разложить их на
CPU / PPU / APU. Снаружи это не видно (`bus.tick()` зовётся изнутри
`mem.read`/`mem.write`), поэтому нужен **вендоринг runes**: скопировать
исходники 0.2.5 в репозиторий как path-зависимость и поставить счётчики внутрь.

Гипотезы, которые проверяются сразу после вендоринга:

1. **Сколько стоит APU.** `bus.tick()` тикает его 29 780 раз за кадр —
   счётчики, envelope, sweep, длины. Раньше это был кандидат на бесплатное
   выключение, но после этапа 12 звук есть и APU нужен, так что вопрос теперь
   не «выключить», а «сколько именно он ест» — если много, оптимизировать
   сам APU (например, считать огибающие не каждый такт, а по событиям).
2. **Потактовый PPU.** 222 инструкции на точку. Рендер по scanline с кэшем
   тайлов принципиально дешевле — именно так был сделан `ppu.c` в удалённом
   nesrecomp. Но это переписывание PPU и потеря потактовой точности.
3. **`&mut dyn` в горячем пути.** Оценка ~1-2 мс из 27, то есть само по себе
   цели не даёт, но после вендоринга это уже дёшево сделать.

Про дисплей: для правильной скорости игры он не нужен вообще. Если `emu`
опустится до 16.6 мс, а flush остаётся 29.5 мс — флашим каждый второй кадр,
игра идёт в реальном времени, картинка обновляется на 30 Гц (двойной буфер это
уже позволяет, DMA укладывается в два кадра эмуляции). Параллельный 8080 нужен
только чтобы видеть все 60 кадров.

## Этап 12: звук через PWM (сделано)

APU в runes уже считает сэмплы и зовёт `Speaker::queue(i16)` на 44100 Гц —
раньше мы их выбрасывали в `NoAudio`. Теперь `PwmSpeaker` превращает сэмпл в
скважность PWM7.

Подключение:

```
PD22 ──[470 Ом]── динамик ── GND
```

PD22 это **гребёнка P2, пин 19** (пин 1 той же гребёнки — GND). Проверено по
отрендеренной схеме, не по текстовому слою PDF. Мультиплексор:
`PD22_SELECT = 0101` → PWM7.

Резистор обязателен: динамик 8 Ом напрямую это ~400 мА, пин отдаёт 10-20 мА.

**На PD22 висит синий светодиод DS2** (VCC3V3 → диод → 5.1К → PD22, то есть
пин в нуле = диод горит). Это бесплатный индикатор: `pwm::led_test()` мигает
им, и если диод мигает — канал PWM доходит до ножки. Отделяет проблемы
конфигурации от проблем обвязки, анализатор не нужен.

Настройки PWM (`pwm.rs`): источник APB0 без деления, ENTIRE_CYCLE = 255 →
период 256 тактов → ровно 8 бит скважности, несущая ~390 кГц. Фильтр не нужен,
несущую гасит сама катушка динамика. Тишина — это 128 (50%), потому что сигнал
двуполярный.

Стоимость: **нулевая**. `fps=33 emu=27ms` не изменились — 24 тысячи записей
регистра в секунду на фоне 19.6 млн инструкций на кадр не видны.

### Замеры звука

- **Амплитуда.** Поле `peak=` в строке лога — пик |сэмпла| до усиления.
  Средний пик по окну на Mario — 12089, максимум в музыке — 23027 из 32768.
  APU занимает ~70% шкалы, запаса в софте почти нет: `GAIN = 4` загонял в
  ограничитель даже средние места. Стоит 2.
- **Тишина на заставке — это не поломка.** Первый прогон дал `peak=1` весь
  лог, и выглядело как неработающий звук. На самом деле кнопки не подключены
  (`NoInput` возвращал «всё отпущено»), Mario стоял на заставке, а там музыки
  нет. После автонажатия START (`input::AutoStart`, кадр 120) `peak` уходит в
  11000-23000 начиная с кадра ~270 — игра стартовала и заиграла тема 1-1.

### Про темп

Сэмплы привязаны к эмулируемому времени, а эмуляция идёт на 55% реальной
скорости. Значит звук ниже и медленнее примерно вдвое — это тот же самый
разрыв в 26 мс, он уйдёт сам, когда эмуляция выйдет на 60 FPS.

### Что дальше по громкости

На 470 Ом динамик получает 8/478 напряжения — десятые доли милливатта, слышно
только вплотную. Варианты: резистор 150-220 Ом (+7...+10 дБ, ток 15-22 мА, пин
выдержит) или модуль PAM8403 (тогда PD22 → RC 1 кОм + 10 нФ → вход модуля,
питание 3.3-5 В, динамик между OUT+ и OUT−, землю на OUT− сажать нельзя —
выход мостовой).

## Конвенции
- `no_std`, `no_main`, `riscv_rt::entry`
- Регистры только через `read_volatile`/`write_volatile`
- Задержки — `utils::delay` (spin-loop)
- Отладка — `println!` в UART0 (115200 8N1)
- Прошивка: `./run.sh` (перезапуск платы перед каждым запуском)
