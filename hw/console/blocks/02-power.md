# Блок 2 — ПИТАНИЕ

Правила и структура — [`../README.md`](../README.md). Всё ниже собрано только из
даташитов, схемы MangoPi и платы Xassette. Строки без ссылки на источник —
только в §7 «Допущения».

Даташиты: `docs/mangopi/f133_datasheet_v1.2.pdf` (F133-A, V1.2),
`docs/chips/SY8089AAAC_C78988.pdf` (AN_SY8089/A Rev. 0.9A),
`docs/chips/TP4056_C16581.pdf` (REV_2.4).

## 1. Микросхемы

| CHIPS.md | Что | Роль в блоке |
|---|---|---|
| §2 | `SY8089AAAC` ×3 | три рельсы: 1.8 / 3.3 / 0.9 В |
| §3 | `TP4056` | заряд банки от USB |
| §7 | движковый выключатель | запуск последовательности |
| §8 | банка Li-Ion с платой защиты | источник `VSYS` |
| §10 | power path — **не ставим** | `BAT` = `VSYS`, один узел |

Топология: `VBUS → TP4056 → BAT = VSYS → движок → EN бакóв`. `VBUS` приходит
из блока 3 (USB), `RESET` уходит в блок 7.

## 2. Из даташита

### 2.1 F133-A: домены и напряжения (Table 5-2, стр. 42–43)

| Символ | Вывод | Min | Typ | Max |
|---|---|---|---|---|
| `VCC-IO` | 83 | **2.97** | 3.3 | 3.63 |
| `VCC-PD` / `VCC-PE` / `VCC-PG` | 66 / 34 / 128 | 2.97 | 3.3 | 3.63 |
| `VCC-TVOUT` | 77 | TBD | 3.3 | TBD |
| `LDO-IN` | 29 | 2.4 | 3.3 | 3.6 |
| `AVCC` / `HPVCC` | 89 / 97 | 1.782 | 1.8 | 1.818 |
| `VDD18-DRAM` | 50 | 1.7 | 1.8 | 1.95 |
| `VCC-RTC` / `VCC-PLL` / `VCC-LVDS` / `VCC-TVIN` | 26 / 20 / 65 / 107 | TBD | 1.8 | TBD |
| `VCC-DRAM0/1` | 48, 49 | TBD | 1.8 | TBD |
| `VDD-CORE0/1`, `VDD-SYS0/1/2` | 116, 117, 46, 51, 81 | TBD | 0.9 | TBD |

Там же, первая строка таблицы — предельная температура зависит от того, откуда
питается память:

| Условие | `Ta` max |
|---|---|
| `VCC-DRAM0/1` от внешнего источника | **85 °C** |
| `VCC-DRAM0/1` от внутреннего LDO | **70 °C** |

`AVCC` = 1.782…1.818 В — это допуск ±1%, шире 1.8 В рельса от бака брать
нельзя. `VCC-IO` min 2.97 В — предел по разряду банки (см. §6.6).

### 2.2 F133-A: потребление (Table 5-3, стр. 44–45)

| Рельс | Условие | Max |
|---|---|---|
| `VCC-IO` | GPIO, 3.3 В, N=19 | 114 мА |
| `VCC-PD` | GPIO, 3.3 В, N=23 | 138 мА |
| `VCC-PE` | GPIO, 3.3 В, N=14 | 84 мА |
| `VCC-PG` | GPIO, 3.3 В, N=16 | 96 мА |
| `VCC-IO` | 2 × USB, 3.3 В | 35 мА |
| `VCC-LVDS` | 1.8 В, 700 МГц dual link | 50 мА |
| `VCC-PLL` | 24 МГц осциллятор | 2 мА |
| `VCC-RTC` | 1.8 В | 0.01 мА |
| `VDD-CORE`, `VDD-SYS` | 0.9 В | **TBD** |
| `VCC-DRAM` | SIP 64 MB DDR2 | **TBD** |
| `AVCC` (ADC/DAC) | 1.8 В, 48 кГц | **TBD** |

Общая формула там же: `Imax = N × 6 мА`, максимум 6 мА на вывод.

⚠️ **Ток ядра, памяти и аудио в даташите не указан.** Всё, что про ток 0.9 В и
1.8 В, — оценка, см. §7.

### 2.3 F133-A: порядок включения (§5.13.1 и Figure 5-28, стр. 70–71)

Дословно:

- «The consequent steps in power-on sequence should not start before the
  previous step supplies have been stabilized within 90–110% of their nominal
  voltage»
- «**VCC-RTC must be ramped no later than other power rails.**»
- «**VCC-IO must be ramped before VDD-SYS and VDD-CORE with a minimum delay of
  2 ms.**»
- «VCC-DRAM needs be stable before SDRAM driver initialization.»
- «the RESET signal must be held on low until all other power rails (except
  24 MHz CLK) are stable for **more than 64 ms**»
- «24MHz clock starts oscillating after the RESET signal is released.»

Figure 5-28, ступени сверху вниз: `VCC-RTC` → `VCC-PLL`/`VCC-TVIN`/`VCC-LVDS`/
`VDD18-DRAM` → `VCC-DRAM0/1` → `VCC-PE`/`VCC-PG`/`VCC-IO`/`VCC-PD`/`VCC-TVOUT`/
`LDO-IN` → **T1 > 2 мс** → `VDD-SYS0/1/2` + `VDD-CORE0/1` → 24M CLK → **T2 >
64 мс** → отпустить `RESET`.

То есть все 1.8 В — раньше 3.3 В, 0.9 В — минимум через 2 мс после 3.3 В.

Выключение (§5.13.2): «After the RESET signal goes low, the 24 MHz clock starts
to stop oscillating. No special restrictions for other power rails.» — порядок
снятия питания не нормирован.

### 2.4 SY8089A (AN_SY8089/A Rev. 0.9A)

Electrical Characteristics, стр. 4:

| Параметр | Значение |
|---|---|
| `VIN` | 2.7…5.5 В |
| `VREF` (FB) | 0.588 / **0.600** / 0.612 В |
| `VENH` (rising) | **1.5 В (Min)** |
| `VENL` (falling) | 0.4 В (Max) |
| `VUVLO` | 2.5 В (Max), гистерезис 0.2 В |
| `FOSC` | 1 МГц |
| `ILIM` PFET | 3.5 А (Min) |
| Max duty | 100% |
| `TSS` soft-start | **1.2 мс** |
| `RDISCH` разряд выхода | **50 Ω** |
| `IQ` / `ISHDN` | 55 мкА / 0.1 мкА |
| `TSD` | 160 °C |

Applications Information, стр. 7–8:

- `Vout = 0.6 × (1 + R1/R2)`, «A value of between 10k and 1M is highly
  recommended for both resistors».
- `CIN`: «X5R or better grade ceramic capacitor with 6.3V rating and **more than
  1 pcs 22µF** capacitor can handle this ripple current well».
- `COUT`: «X5R or better grade ceramic capacitor with 6.3V rating and **greater
  than 22µF** capacitance».
  ⚠️ Описание вывода `IN` на стр. 2 говорит мягче — «at least 10µF ceramic
  cap». Даташит сам себе не строг; берём цифру из Applications Information.
- Индуктор: `ISAT,MIN > IOUT,MAX + VOUT(1−VOUT/VIN,MAX)/(2·FSW·L)`, «desirable
  to choose an inductor with **DCR < 50 mΩ**».
- «adding a **22pF** ceramic cap in parallel with R1 may further speed up the
  load transient responses and is thus recommended for applications with large
  load transient step requirements» — и в типовой схеме (Figure 1, стр. 1) этот
  22 пФ нарисован.
- Layout, п. 5: «If the system chip interfacing with the EN pin has a high
  impedance state at shutdown mode and the IN pin is connected directly to a
  power source such as a LiIon battery, it is desirable to add a **pull down
  1MΩ resistor** between the EN and GND pins». Вывод `EN`, стр. 2: «Pull high
  to turn on. **Do not float.**»
- Суффикс `A`: короткое замыкание — не защёлка, а сброс частоты до ~30% и
  лимита до 3.0 А; перенапряжение — «No latch off protection».

Типовая схема Figure 1 (стр. 1): `VOUT` = 1.8 В, `L1` 2.2 мкГн, `R1` 200k,
`R2` 100k, `C1` 22 пФ, `CIN` 22 мкФ, `COUT` 22 мкФ ×2.

### 2.5 TP4056 (REV_2.4)

Выводы: 1 `TEMP`, 2 `PROG`, 3 `GND`, 4 `VCC`, 5 `BAT`, 6 `STDBY`, 7 `CHRG`,
8 `CE`, плюс термопад.

| Параметр | Значение |
|---|---|
| `VCC` | 4.0 / 5 / 8.0 В |
| `VFLOAT` | 4.158 / **4.2** / 4.242 В |
| `IBAT` при `RPROG` = 1.1k | 850 / **1000** / 1150 мА |
| `IBAT` при `RPROG` = 2.2k | 425 / 500 / 575 мА |
| `ITERM` (C/10) при 1.1k / 2.2k | 130 мА / 70 мА |
| `ITRIKL` (при `VBAT` < `VTRIKL`) | 130 мА |
| `VTRIKL` | 2.8 / 2.9 / 3.0 В |
| `VUV` (UVLO по `VCC`) | 3.5 / 3.7 / 3.9 В |
| `VASD` (`VCC`−`VBAT`, падение) | 5 / 30 / 50 мВ |
| `ΔVRECHRG` | 100 / 150 / 200 мВ |
| `RON` (`VCC`→`BAT`) | 650 мΩ |
| `TLIM` | 145 °C |
| `IBAT` в standby / sleep | −2.5 мкА / −1 мкА |

Формулы и текст:

- `IBAT = (VPROG/RPROG) × 1100`, `RPROG = 1100/IBAT`, таблица на стр. 9
  доводит до 1.1k = 1000 мА.
- «The temperature sense function can be disabled by **grounding the TEMP
  pin**.»
- «A **logic high on CE** pin will put the device in normal operation mode.»
- «When VCC is within 30mV of the BAT pin voltage, the TP4056 enters shutdown
  mode dropping BAT pin's current to less than 2µA.»
- «**No blocking diode or external current sense resistor is required.**»
- `CHRG` (7) и `STDBY` (6) — open-drain, `VOL` 0.3 В при 5 мА.
- ⚠️ «In this state all loads on the BAT pin must be supplied by the battery» —
  после завершения заряда чип перестаёт давать ток совсем, до порога дозаряда
  4.05 В.

Типовая схема №2 (стр. 3, «charging status indicator but no battery temperature
monitoring»): 10 мкФ на `VCC`, 10 мкФ на `BAT`, `CE` соединён с `VCC`, два
светодиода на `CHRG`/`STDBY` с резисторами **1k** на `VCC`.

## 3. MangoPi (`docs/mangopi/mq_sch_v1.6.pdf`, стр. 1)

Один `RY1303` — тройной buck, QFN-20. Прочитано кропами страницы 1.

| Канал | `SW` | `L` | Делитель `FB` | Расчёт | Рельс |
|---|---|---|---|---|---|
| 1 | 11 | `L1` 2.2 мкГн | `R8` 680K 1% / `R9` 150K 1% | 0.6×(1+680/150) = **3.32 В** | `VCC3V3` |
| 2 | 6 | `L3` 2.2 мкГн | `R10` 300K 1% / `R11` 150K 1% | 0.6×(1+300/150) = **1.80 В** | `VCC-DRAM` («DRAM 1.8V») |
| 3 | 20 | `L2` 2.2 мкГн | `R12` 75K 1% / `R13` 150K 1% | 0.6×(1+75/150) = **0.90 В** | `VCC-CORE` («CORE 0.9V») |

На схеме подписано `VF=0.6V` — та же опора, что у SY8089.

Конденсаторы: вход `C25`, `C26`, `C27`, `C76` — **4 × 10 мкФ** на `VCCIN`;
каждый выход — пара **10 мкФ + 0.1 мкФ** (`C28`/`C29`, `C30`/`C31`,
`C32`/`C33`).

**Включение** (кроп узла `EN`):

- `VIN1` (10), `VIN2` (7), `VIN3` (19), `VCC` (16), **`EN1` (9) и `EN2` (8)** —
  всё один узел `VCCIN`. То есть 3.3 В и 1.8 В стартуют одновременно с подачей
  входного питания, без задержки.
- `EN3` (18): `R1` 100K на `VCC3V3`, `R2` 100K на землю, `C4` 0.1 мкФ на землю.
  Установившееся напряжение `VCC3V3/2` = 1.65 В, постоянная времени
  (100K‖100K)×0.1 мкФ = **5 мс**. Рядом подпись «**PWR-ON Sequence wait 2ms**».
- `R45` (между `VCCIN` и узлом `EN3`) — **NC**, не установлен.
- `R23` 100K соединяет `VCCIN` с `VCC3V3`.

**Домены F133 на стр. 3:** `VCC-RTC` (26) и `VCC-PLL` (20) ← **`LDOA-OUT`**
(внутренний LDO, `LDO-IN` (29) ← `VCC3V3`); `VDD18-DRAM` (50) ← `LDOA-OUT`;
`VCC-DRAM0/1` (48, 49) ← внешний buck; `DZQ` (47) ← `R42` 240R.

Зарядника и выключателя на MangoPi нет — плата питается от USB.

## 4. Xassette (`../../ref/Xassette-Asterisk/`, нетлистер `../tools/xassette_net.py`)

Три `SY8089AAAC` — `U3`, `U4`, `U5`. Все три входа `IN` (вывод 4) на **`+5V`**
(USB, батареи на плате нет).

| | `U3` | `U4` | `U5` |
|---|---|---|---|
| Индуктор | `L1` 2.2 мкГн | `L2` 2.2 мкГн | `L3` 2.2 мкГн |
| Делитель `FB` | `R8` 68k / `R7` 15k | `R15` 200k / `R13` 100k | `R14` 200k / `R16` 100k |
| Расчёт | 0.6×(1+68/15) = **3.32 В** | 0.6×(1+200/100) = **1.80 В** | 0.6×(1+100/200) = **0.90 В** |
| Рельс | `+3V3` | `+1V8` | `VDD-CORE` |
| Выходные C | `C2` 10 мкФ + `C3` 0.1 мкФ | `C6` 10 мкФ + `C8` 0.1 мкФ | `C7` 10 мкФ + `C9` 0.1 мкФ |

Вход: шесть 10 мкФ на `+5V` (`C4`, `C10`, `C11`, `C37`, `C38`, `C46`).
22 пФ параллельно верхнему резистору делителя **нет ни на одном канале**.

**Включение:**

- `EN` `U3` ← `R6` 1k от `+5V` — 3.3 В поднимается сразу.
- `EN` `U4` и `EN` `U5` — **один узел**: `R11` 10k от `+3V3`, `C12` 0.1 мкФ на
  землю. τ = 1 мс, порог 1.5 В при источнике 3.3 В достигается за 0.79τ ≈
  **0.8 мс**. То есть 1.8 В и 0.9 В стартуют вместе, примерно через 0.8 мс
  после 3.3 В.

Подтяжки 1M на `EN` нет — выключателя на плате тоже нет, `EN` всегда привязан
к рельсе.

**Домены F133:** `+1V8` → `VCC-PLL` (20), `VCC-RTC` (26), `VDD18-DRAM` (50),
`VCC-LVDS` (65), `AVCC` (89), `HPVCC` (97), `VCC-TVIN` (107); `+3V3` →
`VCC-IO` (83), `VCC-TVOUT` (77); **`VCC-DRAM0/1` (48, 49) ← `LDOB-OUT`**
(внутренний LDO); `VDD-CORE` → 46, 51, 81, 116, 117.

`GPADC0` (101) — **никуда не подключён**, делителя батареи нет.

## 5. Расхождения

| Вопрос | Даташит F133 | MangoPi | Xassette |
|---|---|---|---|
| Кто стартует первым | 1.8 В (`VCC-RTC` «no later than other rails») | 3.3 В и 1.8 В вместе | **3.3 В** |
| Задержка `VCC-IO` → 0.9 В | > 2 мс | ≈ 5 мс (RC, подписано «wait 2ms») | ≈ 0.8 мс — **меньше 2 мс** |
| `VCC-DRAM0/1` (48, 49) | 85 °C снаружи / 70 °C от LDO | **внешний buck** | `LDOB-OUT` — теряет 15 °C |
| `VDD18-DRAM` (50) | 1.7…1.95 В | `LDOA-OUT` | **внешний `+1V8`** |
| `VCC-RTC` (26) | раньше всех | `LDOA-OUT` ← `LDO-IN` ← 3.3 В | внешний `+1V8` |
| `CIN` / `COUT` | SY8089: 22 мкФ / > 22 мкФ | 10 мкФ | 10 мкФ |
| 22 пФ на `FB` | есть в типовой схеме | нет | нет |

Три вывода отсюда:

1. **Ни одна плата не поднимает 1.8 В первой, и это не нарушение.** Даташит
   требует только «`VCC-RTC` не позже остальных» — одновременный старт этому
   удовлетворяет. У MangoPi 3.3 и 1.8 В включаются вместе с подачей питания,
   у Xassette 1.8 В заведена от 3.3 В и отстаёт. Берём MangoPi: обе рельсы
   вместе (§6.2).
2. **Ни одна плата не сделала обе DRAM-рельсы внешними.** MangoPi прав по
   `VCC-DRAM0/1` (те самые 85 °C), Xassette — по `VDD18-DRAM`. Берём внешнее
   и там, и там.
3. **Конденсаторы у обеих плат меньше, чем требует даташит SY8089.** У Xassette
   вход +5 В от USB, а не банка 4.2 В; у MangoPi другой чип со своими
   требованиями. Копировать 10 мкФ не с чего — идём по даташиту.

## 6. Что рисуем

### 6.1 Три бака

Все три — `SY8089AAAC`, вход каждого на `VSYS`.

| | `+1V8` | `+3V3` | `+0V9` |
|---|---|---|---|
| `R1` (верх, `FB`→выход) | 300k 1% | 680k 1% | 75k 1% |
| `R2` (низ, `FB`→GND) | 150k 1% | 150k 1% | 150k 1% |
| Расчёт (§2.4) | 0.6×3 = **1.800 В** | 0.6×(1+680/150) = **3.32 В** | 0.6×1.5 = **0.900 В** |
| Индуктор | 2.2 мкГн | 2.2 мкГн | 2.2 мкГн |
| `CIN` | 22 мкФ | 22 мкФ | 22 мкФ |
| `COUT` | 22 мкФ + 0.1 мкФ | 22 мкФ + 0.1 мкФ | 22 мкФ + 0.1 мкФ |
| 22 пФ ‖ `R1` | нет | нет | **есть** |

- Делители — номиналы MangoPi (§3), все три канала: `150k` внизу везде,
  сверху `300k` / `680k` / `75k`. У `SY8089` та же опора 0.6 В (§2.4), и все
  номиналы в рекомендованном коридоре 10k…1M. Точность 1% — как у MangoPi.
- 3.32 В вместо 3.30 — внутри `VCC-IO` 2.97…3.63 В (§2.1), так на обеих платах.
- `CIN`/`COUT` 22 мкФ — прямое требование §2.4; 0.1 мкФ на выходе повторяет
  обе платы (§3, §4).
- 22 пФ только на 0.9 В: рекомендация §2.4 привязана к «large load transient
  step», а броски тока даёт ядро CPU. На 1.8 и 3.3 В обе платы обходятся без
  него (§5).
- Индуктор — одна позиция на три канала: **2.2 мкГн, ISAT ≥ 2 А, DCR < 50 мΩ**.
  2.2 мкГн — у обеих плат и в типовой схеме; DCR — прямо из §2.4; ISAT
  посчитан в §7.2 (требуется ≤ 0.9 А, берём с запасом до тока насыщения
  самого чипа — `ILIM` 3.5 А).

Расположение: `CIN`, `L`, `R1`, `R2` вплотную к корпусу; `R1`/`R2` и дорожка к
`FB` не рядом с `LX` — §2.4, Layout Design, пп. 1–4. Это указания для разводки,
на схеме отражаются только группировкой.

### 6.2 Секвенсор

Порядок — по MangoPi (§3): **3.3 и 1.8 вместе → (T1) → 0.9**.

```
VSYS ──[движок]──┬──[1k]──► EN (+1V8)
                 │
                 ├──[1k]──► EN (+3V3)
                 │
               [1M]
                 │
                GND

+3V3 ──[47k]──┬──► EN (+0V9)
          [0.47µ]
              │
             GND
```

Даташит F133 (§2.3) ставит на порядок ровно два условия, и оба выполняются:
`VCC-RTC` не позже остальных рельс — она на `+1V8`, которая стартует
одновременно с 3.3 В; `VCC-IO` раньше `VDD-SYS`/`VDD-CORE` минимум на 2 мс —
это и есть T1. Требования «1.8 В раньше 3.3 В» в даташите нет.

Ступень 0.9 В берётся **с рельсы 3.3 В**, а не с узла движка. Причина: RC,
подвешенный к узлу движка, после размыкания подпирает этот узел обратно через
свой резистор, и `EN` баков не отпускается — приставка не гаснет. С рельсы
конденсатор разряжается сам: у `SY8089` при выключении выход замыкается
разрядным ключом 50 Ω (§2.4).

Расчёт по `V(t) = V∞(1 − e^(−t/τ))`, τ = 47k × 0.47 мкФ = **22.1 мс**,
`V∞` = 3.3 В:

| Порог `EN` | t до порога | Что это за граница |
|---|---|---|
| 1.5 В = `VENH` Min | 22.1 × ln(3.3/1.8) = **13.4 мс** | выше этого бак заведомо включён |
| 0.4 В = `VENL` Max | 22.1 × ln(3.3/2.9) = **2.9 мс** | ниже этого бак заведомо выключен |

Типового порога `EN` даташит `SY8089A` не даёт, только эти две границы (§2.4).
Реальный порог лежит между ними, поэтому **T1 = 2.9…13.4 мс** — по нижней
границе, то есть в самом плохом случае, всё равно больше 2 мс ✓. С разбросом
конденсатора −10% нижняя граница 2.6 мс, запас сохраняется.

Полный старт: 3.3 и 1.8 В готовы через 1.2 мс (soft-start), 0.9 В — не позже
13.4 + 1.2 + 1.2 ≈ **16 мс**.

Почему `EN` 3.3 В не взят через тот же RC, что у MangoPi: у них делитель
100K/100K даёт установившиеся 1.65 В при пороге `RY1303` 1.2 В (§3), а у
`SY8089` порог 1.5 В — запас 0.15 В, слишком тонко. Поэтому 3.3 В и 1.8 В
включаются напрямую с узла движка, а RC остался только на 0.9 В, где `V∞` = 3.3 В.

`1M` вниз на узле движка — §2.4, Layout Design п. 5: у нас `IN` подключён прямо
к Li-Ion, а разомкнутый движок оставляет `EN` в воздухе, что даташит запрещает
(«Do not float»). Через контакты движка текут микроамперы, поэтому переходное
сопротивление роли не играет.

Расчёт T1 считает, что 3.3 В поднимается ступенькой. На деле она идёт с
soft-start 1.2 мс, поэтому реальная задержка чуть больше расчётной — в сторону
запаса по T1.

### 6.3 Куда идут рельсы

| Рельс | Выводы F133 |
|---|---|
| `+3V3` | `VCC-IO` 83, `VCC-PD` 66, `VCC-PE` 34, `VCC-PG` 128, `VCC-TVOUT` 77, `LDO-IN` 29 |
| `+1V8` | `VCC-RTC` 26, `VCC-PLL` 20, `VCC-LVDS` 65, `VCC-TVIN` 107, `AVCC` 89, `HPVCC` 97, `VDD18-DRAM` 50, **`VCC-DRAM0/1` 48, 49** |
| `+0V9` | `VDD-CORE0/1` 116, 117, `VDD-SYS0/1/2` 46, 51, 81 |

`VCC-DRAM0/1` от внешнего бака — ради 85 °C вместо 70 °C (§2.1). `LDOA-OUT`
(28) и `LDOB-OUT` (30) при этом не нагружаем, только развязать —
конденсаторы в блоке 8.

**Аналог кодека (`AVCC` 89, `HPVCC` 97) и `VCC-TVIN` (107) — на общей `+1V8`,
как у Xassette.** Здесь платы расходятся: у MangoPi аналоговая 1.8 В сделана
отдельным линейником `U2 XC6206-1.8V` от `VCC3V3` (выход `AVCC1.8`, `C5`
0.1 мкФ + `C22` 2.2 мкФ на `AGND`), и `VCC-TVIN` подключён к нему же через
`R71` 0R. У Xassette выводы 89, 97 и 107 сидят прямо на `+1V8` с бака — и это
рабочий вариант: её README сообщает, что плата играет звук в наушники и пишет
с микрофона. Лишний корпус не ставим. Чем рискуем — §7.8.

`AGND` (91) — на `GND` через 0 Ω в одной точке. Так на обеих платах (`R10` у
Xassette, `R15` у MangoPi). Резистор относится к обвязке F133, блок 8.

Сама развязка по выводам (0.1 мкФ на каждый) — блок 8, здесь только рельсы.

### 6.4 Заряд

`TP4056`, схема — типовая №2 из §2.5 (индикаторы есть, термоконтроль нет):

| Вывод | Куда |
|---|---|
| 1 `TEMP` | **GND** — термоконтроль выключен (§2.5); у нас банка с готовой платой защиты, NTC наружу не выведен |
| 2 `PROG` | 1.1k на GND → `IBAT` = 1000 мА, порог окончания 130 мА (§2.5) |
| 3 `GND` + термопад | земля, полигон под термопадом |
| 4 `VCC` | `VBUS` (блок 3), 10 мкФ на GND |
| 5 `BAT` | `VSYS` = плюс банки, 10 мкФ на GND |
| 6 `STDBY` | светодиод зелёный + 1k на `VBUS` |
| 7 `CHRG` | светодиод красный + 1k на `VBUS` |
| 8 `CE` | на `VCC` (§2.5: «logic high … normal operation mode») |

Светодиоды питаются от `VBUS`, а не от `VSYS`: без кабеля они гаснут и банку
не разряжают.

Ток 1 А выбран в CHIPS.md §3 при условии «заряжаем выключенной приставкой» —
тогда нагрузки на `BAT` нет и порог C/10 достижим (§2.5: после завершения
заряда «all loads on the BAT pin must be supplied by the battery»).

⚠️ Тепло: 1 А × (5 − 3.7) = **1.3 Вт** на корпусе ESOP-8. `TLIM` 145 °C сам
снизит ток, но под термопад нужен полигон меди — иначе заряд всё время упрётся
в температуру.

Диода и токового шунта не ставим — §2.5, «No blocking diode or external current
sense resistor is required». Power path не ставим — CHIPS.md §10.

### 6.5 Контроль напряжения банки

`GPADC0` (вывод 101). Из Table 5-7 (стр. 47): 12 бит, SAR, **полная шкала
0…`AVCC`**, то есть 0…1.8 В, время преобразования 14 тактов при `FADC` ≤ 1 МГц.

Делитель `VSYS` → 100k / 68k на землю, плюс 0.1 мкФ на выводе:

| `VSYS` | На `GPADC0` |
|---|---|
| 4.2 В (полный заряд) | 4.2 × 68/168 = **1.70 В** |
| 3.4 В (порог гашения) | **1.376 В** |
| 3.0 В (защита банки, CHIPS.md §8) | 1.214 В |

1.70 В при полной шкале 1.8 В — 94% диапазона, без выхода за шкалу. Ток через
делитель 25 мкА постоянно.

У Xassette `GPADC0` не подключён (§4). У MangoPi батареи нет, и вывод 101 там
подтянут `R29` 100K к `AVCC1.8` — то есть к полной шкале, а не к делителю
измерения. Референса для измерения банки нет ни на одной плате, только даташит.

### 6.6 Что этот блок не решает

- **Отсечка по разряду.** `VCC-IO` min 2.97 В (§2.1), а бак при 100% duty
  (§2.4) ниже ~3.4 В на входе тянет рельс за банкой. Значит гасить консоль
  должна прошивка по `GPADC0` около 3.4 В — раньше, чем сработает защита банки
  на 2.4…3.0 В (CHIPS.md §8).
- **`RESET`.** T2 > 64 мс после того, как поднялись все рельсы (§2.3). Наш
  старт занимает ≈ 16 мс (§6.2), значит `RESET` должен отпускаться не раньше
  ≈ 80 мс от замыкания движка. Цепь `RESET` — блок 7, домен `VCC-RTC` = 1.8 В.
- **`VBUS`** приходит из блока 3.
- **Питание усилителя** — от `VSYS`, а не от 3.3 В (CHIPS.md §5), блок 5.

### 6.7 Состав блока

| Позиция | Кол-во | Номинал | Откуда |
|---|---|---|---|
| `SY8089AAAC` SOT-23-5 | 3 | | CHIPS.md §2 |
| `TP4056` ESOP-8 | 1 | | CHIPS.md §3 |
| Индуктор | 3 | 2.2 мкГн, ISAT ≥ 2 А, DCR < 50 мΩ | §6.1 |
| C вход баков | 3 | 22 мкФ 0805 X5R 6.3 В | §2.4 |
| C выход баков | 3 | 22 мкФ 0805 X5R 6.3 В | §2.4 |
| C выход баков | 3 | 0.1 мкФ | §3, §4 |
| R делителей `FB` | 6 | 1%: 300k, 680k, 75k, 150k ×3 | §6.1 |
| C ‖ `R1` на 0.9 В | 1 | 22 пФ | §2.4 |
| R секвенсора | 4 | 1k ×2, 1M, 47k | §6.2 |
| C секвенсора | 1 | 0.47 мкФ | §6.2 |
| Движковый выключатель | 1 | | CHIPS.md §7 |
| C на `VCC`/`BAT` TP4056 | 2 | 10 мкФ | §2.5 |
| R `PROG` | 1 | 1.1k | §2.5 |
| Светодиод + R | 2 + 2 | 1k | §2.5 |
| R делителя батареи | 2 | 100k, 68k | §6.5 |
| C на `GPADC0` | 1 | 0.1 мкФ | §6.5 |
| Перемычка 0 Ω в разрыв рельсы | 3 | 0 Ω 0805 | ТЗ, требование 5 |
| Разъём банки | 1 | `PinHeader_1x02_P2.54mm`, как `J3` в v1 | ТЗ, состав |
| Контрольные точки | 5 | `VSYS`, `+3V3`, `+1V8`, `+0V9`, `GND` | — |

Итого 22 мкФ — **6 шт**, это и записано в CHIPS.md «Что покупать».

Разъём банки в ТЗ отдельной строкой не значился, но без него аккумулятор не
подключить, а `VSYS` рождается именно здесь — поэтому он в блоке. Берём тот же,
что на плате v1 (`J3` «Batt», `Connector:Conn_01x02_Pin`, гребёнка 1×2 с шагом
2.54 мм, вывод 1 — плюс банки, вывод 2 — земля): он есть в закромах
(BOARD-draft.md §8).

Блок нарисован скриптом `../tools/block02_power.py` (запуск повторно —
идемпотентно, содержимое рамки блока 2 переписывается целиком). Ссылка на
раздел этого документа лежит в поле `Источник` каждого компонента схемы.
Пассив — 0805 в исполнении HandSolder. Корпуса дросселей и движка не заданы,
пока не выбраны сами детали (ТЗ, «осталось выбрать») — пустое поле `Footprint`
видно в ERC и не даст про них забыть.

## 7. Допущения и открытые вопросы

### 7.1 Токи рельсов — оценка, не факт

Даташит даёт максимумы только по GPIO и мелочи (§2.2); ток ядра, памяти и
аудиокодека стоит **TBD**. Что берём:

| Рельс | Оценка | Как получена |
|---|---|---|
| `+3V3` | ≈ 0.5 А | сумма максимумов GPIO из §2.2 (114+138+84+96+35 = 467 мА) — это худший случай, когда все выводы переключаются на 6 мА |
| `+1V8` | ≈ 0.3 А | `VCC-LVDS` 50 мА + `VCC-PLL` 2 мА из §2.2, всё остальное (DDR2 IO, кодек) — **допущение** |
| `+0V9` | ≈ 0.6 А | **допущение целиком**, в даташите TBD |

Чем рискуем: если ядро потребляет заметно больше 0.6 А — вырастет пульсация и
понадобится индуктор с большим `ISAT`. Проверяется на живой плате измерением, а
не расчётом. Запас есть: сам чип отдаёт 2 А непрерывно, `ILIM` 3.5 А (§2.4).

### 7.2 `ISAT` индуктора

По формуле §2.4 при `VIN,MAX` = 4.2 В, `FSW` = 1 МГц, `L` = 2.2 мкГн:

| Рельс | ΔI = `VOUT`(1−`VOUT`/`VIN`)/(`FSW`·`L`) | `ISAT,MIN` = `IOUT` + ΔI/2 |
|---|---|---|
| 3.3 В | 0.32 А | 0.5 + 0.16 = 0.66 А |
| 1.8 В | 0.47 А | 0.3 + 0.24 = 0.54 А |
| 0.9 В | 0.32 А | 0.6 + 0.16 = 0.76 А |

`IOUT` здесь — оценки из §7.1, поэтому и результат оценка. Требование
**ISAT ≥ 2 А** взято не из этой таблицы, а из тока самого чипа: 2 А
непрерывно (§2.4). Тогда номинал индуктора не придётся пересматривать, чем бы
ни оказался реальный ток.

### 7.3 Нижняя граница T1

`EN` баков 1.8 и 3.3 В сидят прямо на узле движка, там запас по порогу полный
(`VSYS` ≥ 3.0 В против `VENH` 1.5 В). Открыт другой вопрос: точный порог `EN`
для ступени 0.9 В. Даташит даёт только `VENH` Min 1.5 В и `VENL` Max 0.4 В,
типового значения и температурной зависимости нет (§2.4), поэтому T1 известен
лишь как диапазон 2.9…13.4 мс (§6.2). Нижняя граница выше требуемых 2 мс, так
что решение защищено при любом реальном пороге, но фактическую задержку надо
померить на плате осциллографом. Если окажется мало — увеличивается `C`, номинал
на схеме один.

### 7.4 `R23` на MangoPi

100K между `VCCIN` и `VCC3V3` (§3). Зачем — из схемы не следует, подписи нет.
Не копируем.

### 7.5 22 пФ на `FB` 0.9 В

Ни одна референсная плата его не ставит (§5). Основание — только типовая схема
и текст даташита SY8089. Деталь дешёвая и снимается перемычкой, риск нулевой.

### 7.6 0.1 мкФ на `GPADC0`

Даташит F133 не задаёт ни входное сопротивление, ни ёмкость выборки GPADC
(Table 5-7 их не содержит), референса нет ни на одной плате. Конденсатор
поставлен как обычная антиалиасинговая ёмкость при источнике 40k. Если
измерения поплывут — подбирается на живой плате.

### 7.7 Аналог кодека на общей рельсе

Решение из §6.3: `AVCC`/`HPVCC` берутся с бака, отдельного линейника нет.
Подтверждение — Xassette, где так и сделано и звук работает. Чем это отличается
от MangoPi, кроме самого линейника: у Xassette после кодека сразу гнездо
наушников (`R29`/`R30` 2R2 + 0.1 мкФ), а у нас, как у MangoPi, стоит `PAM8301`
с жёстким усилением 24 дБ (CHIPS.md §5). Всё, что пролезет с рельсы через PSRR
кодека на `HPOUT`, усилитель умножит на 16. PSRR аналоговых блоков F133 в
даташите не указан, посчитать нельзя — проверяется только на живой плате.

Если зашумит: `SD` усилителя уже заведён на GPIO (CHIPS.md §5), так что источник
локализуется без перепайки — глушим усилитель и слушаем, остаётся ли фон.
Запасной путь на случай, если дело в рельсе, — `MAX98357A` с цифровым входом
I²S, он аналоговый тракт не трогает вообще (CHIPS.md §5, лежит в корзине).

### 7.8 Что не проверено ничем

- Реальный ток заряда при работающей приставке (см. §6.4) — режим заведомо
  кривой, принят осознанно в CHIPS.md §3/§10.
- Тепловой режим TP4056 при 1 А — зависит от полигона, считается при разводке.
- Пороги гашения по `GPADC0` (3.4 В) — выбраны по `VCC-IO` min 2.97 В, точное
  значение подбирается на плате.
- **Ёмкость `COUT` под смещением.** Даташит требует «greater than 22µF» (§2.4), у
  нас стоит 22 мкФ + 0.1 мкФ — формально проходит, но по номиналу. Типовая схема
  того же даташита ставит 22 мкФ ×2, и у X5R 6.3 В под рабочим смещением
  реальная ёмкость заметно ниже номинала. Кривой DC bias для конкретного
  конденсатора нет — он появится только вместе с партномером. Место под второй
  22 мкФ есть на всех трёх каналах, добавляется без переделки.
