# Блок 6 — ДИСПЛЕЙ

Панель 3.5″ ILI9488 480×320 на шлейфе FFC-40 0.5 мм, разведённая **сразу под
два интерфейса**: i8080 16 бит и 4-проводный SPI, переключение одним сигналом
`IM_SEL`. Плюс подсветка через MOSFET на `PD22`.

Блок закрывает строки ТЗ **«Экран»** и **«Подсветка»** и требование 4
(«Экран разведён сразу под оба интерфейса … Софт стартует на SPI, потом
переезжает на 8080 без перепайки»). По BOARD-draft.md §9 это шаг 3.

## 0. Источники этого блока

К трём обычным опорам добавляется **спека самой панели** — она такой же
первоисточник, как даташит любой микросхемы из `docs/chips/`, просто лежит
отдельно:

| | |
|---|---|
| **Даташит ILI9488** | `docs/display/ili9488/ILI9488_DTS_V100.pdf`, 343 стр., ILI Technology, версия V100 |
| Спека панели | `docs/display/ili9488/3.5寸TN_规格书_ILI9488_40PIN.pdf`, 16 стр. |
| Схема применения от того же поставщика | `docs/display/ili9488/3.5-40pin应用电路Protel Schematic.pdf`, 1 лист |
| Даташит F133 | `docs/mangopi/f133_datasheet_v1.2.pdf` — Table 4-3, мультиплексор GPIO |
| **User manual F133** | `docs/mangopi/f133_user_manual_v1.0.pdf` — только здесь есть раскладка шины i8080 по выводам `LCD0-D` |
| Даташит разъёма | `docs/chips/FFC-40p-0.5mm_Hirose_C202114.pdf` — Hirose FH12 |
| MangoPi | стр. 1 (разъём `P1`, драйвер подсветки `U5`), стр. 3 (`R59`) |
| Xassette | `J1`, `U6 SY7201ABC` |

⚠️ **`docs/display/lcd.pdf` — не спека нашей панели**, это application note ST
про контроллер LTDC в STM32. `docs/display/lcd_pins.webp` — распиновка
**другого** 40-выводного шлейфа (24-битный RGB). К ILI9488 оба отношения не
имеют, в блоке не используются.

## 1. Микросхемы

Своей микросхемы нет: контроллер ILI9488 сидит на стекле панели. По CHIPS.md
блок закрывает **§4 «Подсветка экрана — без драйвера, как на стенде»**.

Покупается: разъём FFC-40 (`C202114`, Hirose FH12, $0.70) и N-канальный MOSFET
`AO3400A` (§7.1).

Выводы F133, которые закрывает блок: **весь банк PD целиком**, выводы 52…76 —
23 штуки, плюс `PC2` (19) и `PC4` (17) вторым концом на общей шине SPI0.

## 2. Из даташита

### 2.1 Что за панель (спека §1.0 и §2.0, стр. 3)

| | |
|---|---|
| Матрица | 320 RGB × 480, TFT |
| Контроллер | ILI9488 |
| Интерфейсы | «8080 3/4WIRE-SPI, 8/16bit» |
| Модуль | 54.56 × 84.07 × 2.15 мм, активная область 48.96 × 73.44 мм |
| Точка | 0.153 × 0.153 мм |

### 2.2 Электрика панели (спека §5.0, стр. 9; §9.0, стр. 13)

| Параметр | Мин | Тип | Макс |
|---|---|---|---|
| `VDD` (питание модуля) | 2.7 | **3.3** | 3.5 |
| `VIH` | 0.8 × `VDD` | | `VDD` |
| `VIL` | −0.3 | | 0.2 × `VDD` |
| `IDD` при `VDD` = 3.3 В | | 5 мА | |
| Предел `VDD` (§9.0) | 2.7 | | **3.6** |
| Рабочая температура | −20 | | +70 °C |

Подсветка (§5.2):

| Параметр | Мин | Тип | Макс |
|---|---|---|---|
| `VF` | | **3.2** | **3.5** В |
| Равномерность при `IF` = 90 мА | 80 % | | |

### 2.3 Распиновка шлейфа (спека §6.0, стр. 10–11)

| Вывод | Имя | Что говорит спека |
|---|---|---|
| 1…4 | `XL` `YU` `XR` `YD` | тачскрин; не используем |
| 5, 16, 37 | `GND` | |
| 6 | `IOVCC` | «Power supply for LCM (2.8–3.3 V)», системное |
| 7 | `IOVCC` | то же, логическое |
| 8 | `FMARK` | tearing effect; **«If not used, open this pin»** |
| 9 | `CS` / SPI `CS` | активен низким |
| 10 | `RS` / `SCL`/`SCK` | параллельно — выбор «данные/команда»; последовательно — **тактовый сигнал** |
| 11 | `WR` / `A0` (4-wire) | строб записи по фронту; в 4-проводном SPI — выбор команда/параметр. **«Fix to IOVCC level when not in use»** |
| 12 | `RD` | строб чтения. **«Fix to IOVCC level when not in use»** |
| 13 | SPI `SDI`/`SDA` | вход данных. **«If not used, fix this pin at IOVCC or GND»** |
| 14 | SPI `SDO` | выход данных. **«If not used, open this pin»** |
| 15 | `RESET` | активен низким |
| 17…24 | `DB0`…`DB7` | **«Fix to GND level when not in use»** |
| 25…32 | `DB8`…`DB15` | то же |
| 33 | `LED-A` | анод подсветки, **3.0–3.4 В, типовое 3.2** |
| 34…36 | `LED-K1/K2/K3` | катоды подсветки |
| 38, 39, 40 | `IM0`, `IM1`, `IM2` | выбор интерфейса |

Сноска там же: «D[8:1] — младшие 8 линий `DB7`–`DB0`, D[17:10] — старшие 8
линий `DB15`–`DB8`». То есть `DB15..DB0` контроллера — это стандартная
раскладка RGB565: `DB15..DB11` = R4..R0, `DB10..DB5` = G5..G0, `DB4..DB0` =
B4..B0.

### 2.4 Страпы `IM` (спека Table 3 «Interface Selection», стр. 11)

| `IM2` | `IM1` | `IM0` | Интерфейс |
|---|---|---|---|
| 0 | 0 | 0 | DBI Type B 24-bit (`DB_EN` = 1) |
| 0 | 0 | 0 | DBI Type B 18-bit (`DB_EN` = 0) |
| 0 | 0 | 1 | DBI Type B 9-bit |
| **0** | **1** | **0** | **DBI Type B 16-bit** ← наш i8080 |
| 0 | 1 | 1 | DBI Type B 8-bit |
| 1 | 0 | 1 | DBI Type C Option 1 (3-line SPI) |
| 1 | 1 | 0 | DSI |
| **1** | **1** | **1** | **DBI Type C Option 3 (4-line SPI)** ← наш SPI |

Два следствия, на которых держится весь блок:

- **`IM1` = 1 в обоих режимах** → жёстко на `IOVCC`, ни резистора, ни GPIO;
- **`IM0` = `IM2` в обоих режимах** (0/0 и 1/1) → это **одна цепь** `IM_SEL`,
  один GPIO на оба вывода шлейфа.

Цена второго интерфейса по страпам — **ровно один вывод F133**.

### 2.4.1 То же по даташиту ILI9488 (стр. 23–24)

Таблица выводов контроллера подтверждает Table 3 спеки строка в строку и
добавляет строку `1 1 0 = MIPI DSI`. Оттуда же — описание шины:

| Режим | Задействованные линии |
|---|---|
| MIPI-DBI Type B 24-bit (`DB_EN` = 1) | `DB[23:0]` |
| MIPI-DBI Type B 18-bit (`DB_EN` = 0) | `DB[17:0]` |
| **MIPI-DBI Type B 16-bit** | **`DB[15:0]`** |
| MIPI-DBI Type B 9-bit | `DB[8:0]` |
| MIPI-DBI Type B 8-bit | `DB[7:0]` |

И ниже, для самой строки `DB[23:0]`: **«Fix to DGND level when not in use.»** —
то же требование, что в спеке модуля, но уже от разработчика микросхемы.
Разбор последствий — §7.5.

`RESX`: «Initialize the chip with a low input. **Be sure to execute a power-on
reset after supplying power.**» То есть сброс после подачи питания
обязателен — а значит `R608` не перестраховка, а выполнение прямого требования.

### 2.4.2 Тайминги шины i8080 (даташит §17.4.1, стр. 329–330)

| Сигнал | Символ | Параметр | Мин | Ед. |
|---|---|---|---|---|
| `WRX` | `twc` | цикл записи | **40** | нс |
| | `twrh` | `WR` высокий | 15 | нс |
| | `twrl` | `WR` низкий | 15 | нс |
| `DB[…]` | `tdst` | установка данных | 10 | нс |
| | `tdht` | удержание данных | 10 | нс |
| `D/CX` | `tast` / `taht` | установка / удержание адреса | 0 | нс |
| `CSX` | `tcs` | установка при записи | 15 | нс |
| | `tchw` / `tcsf` | пауза / ожидание | 0 | нс |
| `RDX` | `trc` / `trdl` / `trdh` | чтение ID | 160 / 45 / 90 | нс |
| | `trcfm` / `trdlfm` / `trdhfm` | чтение из памяти кадра | 450 / 355 / 90 | нс |

Условия (примечание 1): `Ta` −30…70 °C, `IOVCC` 1.65…3.3 В, уровни считаются
по 30 % и 70 % от `IOVCC`.

Сводим с §2.7. TCON F133 формирует `WR` как «180° delay of DCLK», то есть
`twc` = период `DCLK`, а `twrl` = `twrh` = полпериода. Ограничения складываются
в одно:

```
twc  ≥ 40 нс  и  twrl = twrh = twc/2 ≥ 15 нс   →   DCLK ≤ 25 МГц
```

Кадр 480 × 320 = 153 600 точек, по одной шестнадцатибитной записи на точку:

| | |
|---|---|
| предел шины | 1 / 40 нс = 25 млн записей/с → **162 кадра/с** |
| нужно на 60 кадров/с | 9.2 млн записей/с, то есть `twc` = 108 нс |
| **запас** | **2.7 раза** |

Требование к настройке: **`DCLK` дисплейного контроллера не выше 25 МГц**.
Пиксельклок для 480 × 320 @ 60 Гц с полями гашения — порядка 12 МГц, то есть в
два раза ниже потолка. Тайминги сходятся с запасом, и §7.9 из «главного
неразобранного риска» превращается в одну строчку в настройках TCON.

Чтение работает медленнее записи: `trcfm` 450 нс на слово из памяти кадра.
Для диагностики (прочитать ID панели) этого более чем достаточно, для обратного
чтения кадра — нет. Нам обратное чтение не нужно.

### 2.4.3 Предел последовательного сопротивления (даташит §18.1, стр. 340)

Отдельная таблица «Maximum layout resistance». Для всех наших цепей —
`DB[23:0]`, `WRX/SCL`, `D/CX`, `CSX`, `RDX`, `SDA`, `SDO`, `IM[2:0]`, `RESX` —
предел **100 Ω**. Для питаний (`IOVCC`, `VCI`, земли) — 5 Ω.

Наши 33 Ω (§6.4) втрое ниже предела. На выводе 10 шлейфа последовательно стоит
только один резистор из двух — вторая ветка в это время в `IO Disable` (§2.9),
так что 33 Ω, а не 66.

### 2.4.4 Сброс панели (даташит §13.4, Table 39 и 40, стр. 308)

| Символ | Параметр | Мин | Макс |
|---|---|---|---|
| `tRW` | длительность импульса сброса | **10 мкс** | |
| `tRT` | снятие сброса | | 5 мс (загрузка настроек из EEPROM) … **120 мс** |

Table 40: импульс короче 5 мкс отбрасывается как помеха, длиннее 9 мкс —
сброс. То есть линия `RESET` защищена от иголок самой микросхемой, RC-цепочка
на ней не нужна — хватает `R608`.

Для софта: **держать `RESET` низким не меньше 10 мкс и ждать после отпускания
до 120 мс**, прежде чем слать первую команду.

### 2.5 Схема применения от поставщика панели

Тот же поставщик, отдельный лист `3.5-40pin应用电路Protel Schematic.pdf`.
Это не пересказ, а чертёж — на нём три варианта подключения и общие замечания.

Примечания в шапке:

1. интерфейс выбирается страпами `IM0/1/2`;
2. `VCC`/`IOVCC` панели — **3.3 В**;
3. **«背光供电：正极A_3.3V, 75mA，负极K1-6可接地，或者接一起联接到一个三极管
   与主控芯片IO软件控制»** — питание подсветки: анод `A` на **3.3 В, 75 мА**,
   катоды `K1-6` можно посадить на землю либо соединить вместе и завести на
   транзистор под управлением GPIO;
4. выводы тачскрина при неиспользовании висят в воздухе.

Панель «使用MCU16位接口» (16-битный MCU-интерфейс) — ровно наш режим:

- `8PIN_FMARK` — в воздухе;
- **`13PIN_SDA` — на GND**;
- `14PIN_SDO` — в воздухе;
- `39PIN_IM1` — на `VCC`;
- `38/40PIN_IM0/2` — на GND.

Панель «使用串口4线SPI接口» (4-проводный SPI):

- **`12PIN_RD` — на `VCC`**;
- `38/39/40PIN_IM0/1/2` — на `VCC`;
- **`17-32PIN_DB0-15` — на GND**;
- и прямо там же красным: **«3.5寸TFT不建议用串口驱动»** — для 3.5″ панели
  последовательный интерфейс **не рекомендуется**.

Схемотехника на листе:

| | |
|---|---|
| `C6` 1.0 мкФ/10 В | на `VCC`/`IOVCC` |
| `R7` **0 Ω** | последовательно с анодом подсветки от 3.3 В |
| `R1`…`R6` | по **два посадочных места на каждый вывод `IM`** — одно на 3.3 В, одно на GND; ставится нужное |
| `Q1` S8050 + `R1` 10k в базу + `R2` 10k на 3.3 В | рекомендуемый ключ подсветки; `BL_CTR` **подтянут вверх**, то есть подсветка по умолчанию включена |

### 2.6 Мультиплексор GPIO F133 (даташит Table 4-3, стр. 28–29)

Функция 2 банка PD — это и есть шина `LCD0`:

| PD | вывод | Function2 | | PD | вывод | Function2 |
|---|---|---|---|---|---|---|
| PD0 | 55 | `LCD0-D2` | | PD12 | 70 | `LCD0-D18` |
| PD1 | 56 | `LCD0-D3` | | PD13 | 69 | `LCD0-D19` |
| PD2 | 57 | `LCD0-D4` | | PD14 | 71 | `LCD0-D20` |
| PD3 | 58 | `LCD0-D5` | | PD15 | 72 | `LCD0-D21` |
| PD4 | 59 | `LCD0-D6` | | PD16 | 73 | `LCD0-D22` |
| PD5 | 60 | `LCD0-D7` | | PD17 | 74 | `LCD0-D23` |
| PD6 | 61 | `LCD0-D10` | | PD18 | 75 | `LCD0-CLK` |
| PD7 | 62 | `LCD0-D11` | | PD19 | 76 | `LCD0-DE` |
| PD8 | 63 | `LCD0-D12` | | PD20 | 54 | `LCD0-HSYNC` |
| PD9 | 64 | `LCD0-D13` | | PD21 | 53 | `LCD0-VSYNC` |
| PD10 | 67 | `LCD0-D14` | | PD22 | 52 | `OWA-OUT` / `IR-RX` / `UART1-RX` / **`PWM7`** |
| PD11 | 68 | `LCD0-D15` | | | | |

Обратить внимание: **`PD12` — вывод 70, `PD13` — вывод 69**, порядок в корпусе
переставлен. То же видно на схеме MangoPi (§3).

Оттуда же — SPI0: `PC2` (19) `SPI0-CLK`, `PC3` (18) `SPI0-CS0`, `PC4` (17)
`SPI0-MOSI` / `BOOT-SEL0`, `PC5` (16) `SPI0-MISO` / `BOOT-SEL1`.

### 2.7 Интерфейс i8080 в F133 (user manual §5.1.3.5, Table 5-7, стр. 372)

| Сигнал | Описание | Тип |
|---|---|---|
| `CS` | Chip select, active low | O |
| `WR` | Write strobe, active low | O |
| `RD` | Read strobe, active low | O |
| `A1` | Address bit, управляется `LCD_CPUI/F` BIT26/25 | O |
| `D[23..0]` | Digital RGB output signal | I/O |

Там же текстом: «WR is 180° delay of DCLK; CS is active when pixel data is
valid; **RD is always set to 1**; A1 is set by `LCD_CPUI/F`». То есть в режиме
8080 все четыре управляющих сигнала формирует железо TCON, софт их не дёргает.
`A1` — это и есть `RS` панели (выбор команда/данные).

Общий список внешних сигналов — Table 5-1, стр. 367: `LCD0-D[23:0]`,
`LCD0-CLK`, `LCD0-VSYNC`, `LCD0-HSYNC`, `LCD0-DE`.

### 2.8 Раскладка i8080 по выводам `LCD0` (user manual §5.1.3.2, стр. 368–369)

**Это ключевая таблица блока.** Широкая таблица «Control Signal and Data Port
Mapping» показывает, во что превращается каждый вывод `LCD0` в каждом режиме
панели. Колонки читались не глазами, а по координатам слов
(`pdftotext -bbox`): заголовок «65K» группы «CPU 16bit» стоит в
x = 365.4…374.5 pt, и в тело таблицы попадают только слова с центром
x ≈ 369.9.

Управление:

| Внешний вывод | SYNC RGB | **CPU (i8080)** |
|---|---|---|
| `LCD0-VSYNC` | `VSYNC` | **`CS`** |
| `LCD0-HSYNC` | `HSYNC` | **`RD`** |
| `LCD0-CLK` | `DCLK` | **`WR`** |
| `LCD0-DE` | `DE` | **`RS`** |

Данные, колонка **CPU 16bit / 65K**:

| `LCD0` | бит | | `LCD0` | бит | | `LCD0` | бит |
|---|---|---|---|---|---|---|---|
| D23 | R4 | | D18 | G5 | | D7 | B4 |
| D22 | R3 | | D15 | G4 | | D6 | B3 |
| D21 | R2 | | D14 | G3 | | D5 | B2 |
| D20 | R1 | | D12 | G2 | | D4 | B1 |
| D19 | R0 | | D11 | G1 | | D3 | B0 |
| | | | D10 | G0 | | | |

Шестнадцать линий: `D3`…`D7`, `D10`…`D12`, `D14`, `D15`, `D18`…`D23`.
Не участвуют `D0`, `D1`, `D2`, `D8`, `D9`, `D13`, `D16`, `D17`.

Сведём с §2.6 и с раскладкой RGB565 панели из §2.3 — и получается **монотонное
соответствие без единого перекрёста**:

| FFC | `DB` | цвет | `LCD0` | PD | вывод F133 |
|---|---|---|---|---|---|
| 17 | `DB0` | B0 | D3 | PD1 | 56 |
| 18 | `DB1` | B1 | D4 | PD2 | 57 |
| 19 | `DB2` | B2 | D5 | PD3 | 58 |
| 20 | `DB3` | B3 | D6 | PD4 | 59 |
| 21 | `DB4` | B4 | D7 | PD5 | 60 |
| 22 | `DB5` | G0 | D10 | PD6 | 61 |
| 23 | `DB6` | G1 | D11 | PD7 | 62 |
| 24 | `DB7` | G2 | D12 | PD8 | 63 |
| 25 | `DB8` | G3 | D14 | PD10 | 67 |
| 26 | `DB9` | G4 | D15 | PD11 | 68 |
| 27 | `DB10` | G5 | D18 | PD12 | 70 |
| 28 | `DB11` | R0 | D19 | PD13 | 69 |
| 29 | `DB12` | R1 | D20 | PD14 | 71 |
| 30 | `DB13` | R2 | D21 | PD15 | 72 |
| 31 | `DB14` | R3 | D22 | PD16 | 73 |
| 32 | `DB15` | R4 | D23 | PD17 | 74 |

`DB0`…`DB15` = `PD1`…`PD8` + `PD10`…`PD17`, единственный разрыв на `PD9`.
Шина уходит от корпуса к разъёму веером, без пересечений — на двухслойной
плате это решает половину разводки блока.

### 2.9 Состояние выводов PD после сброса (user manual §9.7.5.11, стр. 1054)

`PD_CFG0` (0x0090), **default value `0xFFFF_FFFF`**. Значения полей `PDn_SELECT`:

```
0000: Input     0001: Output    0010: LCD0-Dn   ...   1110: PD-EINTn   1111: IO Disable
```

Значит **сразу после сброса каждый вывод банка PD находится в `IO Disable`**,
то есть в третьем состоянии. Из этого следуют три вещи:

- трюк «два вывода F133 на одну линию шлейфа» безопасен: неактивный вывод
  переводится в `1111` и физически отключён от цепи;
- цепи `RESET` и `IM_SEL` панели до старта софта **висят в воздухе** и требуют
  резисторов, задающих уровень;
- вход `CS` панели тоже висит, пока софт не возьмёт `PD21`.

## 3. MangoPi (`docs/mangopi/mq_sch_v1.6.pdf`)

У MangoPi панель **другая** — 24-битная RGB на своём 40-выводном шлейфе, а не
i8080. Поэтому она не образец для нашего разъёма, но три вещи проверяет.

### 3.1 Какие `LCD0-D` вообще выведены на корпус (стр. 1, разъём `P1`)

`P1` подписан по цветам: `R0`, `R1` — **не подключены**, `R2`…`R7` идут на
`LCD_D18`…`LCD_D23`; `G0`, `G1` не подключены, `G2`…`G7` — на
`LCD_D10`…`LCD_D15`; `B0`, `B1` не подключены, `B2`…`B7` — на
`LCD_D2`…`LCD_D7`.

То есть серийная плата на этом же чипе пользуется ровно набором
`D2`…`D7`, `D10`…`D15`, `D18`…`D23` — восемнадцатью линиями. Наш
шестнадцатибитный набор из §2.8 целиком внутри него. Прочие `LCD0-D`
(`D0`, `D1`, `D8`, `D9`, `D16`, `D17`) на F133 в eLQFP128 живут в банке PB
(Table 4-3: `PB2`…`PB7`), и MangoPi их для дисплея не берёт.

### 3.2 Последовательные резисторы (стр. 3)

Читано нетлистером `tools/mangopi_net.py`, потом проверено кропом.

**`R59` 33 Ω стоит ровно на одной цепи — `LCD_CLK`, вывод 75 (`PD18`).**
Соседние `LCD_DE` (76), `LCD_D23` (74), `LCD_D22` (73), `LCD_D21` (72),
`LCD_D20` (71), `LCD_D18` (70), `LCD_D19` (69), `LCD_D15` (68), `LCD_D14` (67)
уходят с выводов напрямую, без единого резистора.

Тот же кроп подтверждает перестановку из §2.6: **70 = `PD12` → `LCD_D18`,
69 = `PD13` → `LCD_D19`**.

### 3.3 Подсветка (стр. 1)

`U5 MT9284-28J` — повышающий драйвер светодиодов: `LCD_PWM` → `EN` (с
подтяжкой `R36` 5.1k на `VCC3V3`), выход `LEDA` на разъём, `LEDK` → `FB` и
`R4` 2R7 на землю. Подписью на схеме — **38 мА**.

Существенно здесь не топология (у них цепочка светодиодов с высоким `VF`), а
то, что **`EN` подтянут вверх**: подсветка по умолчанию включена, как и в §2.5.

Питание панели: `VDD` на `VCC3V3`, развязка `C59` 0.1 мкФ + `C60` 10 мкФ прямо
у разъёма. `DISP` и `NC/PSSEL` посажены на `VCC3V3`.

## 4. Xassette (`../../ref/Xassette-Asterisk/`)

Тоже **другая панель**: `J1` — `Conn_01x40` в футпринте
`TE_4-1734839-0_1x40-1MP_P0.5mm_Horizontal`, но распиновка RGB:
1 `LCD_K`, 2 `LCD_A`, 3 `GND`, 4 `+3V3`, 5…28 `LCD0-D0`…`LCD0-D23`,
29 `GND`, 30 `LCD0-CLK`, 31 не подключён, 32 `LCD0-HSYNC`, 33 `LCD0-VSYNC`,
34 `LCD0-DE`, 35 `NoConn`, 36 `GND`.

Обратите внимание: Xassette заводит на разъём **все 24** линии `LCD0-D`,
включая `D0`, `D1`, `D8`, `D9`, `D16`, `D17` — то есть задействует и банк PB.

Подсветка: `U6 SY7201ABC` — повышающий драйвер от `+5V`, `L5` 10 мкГн, `D2`,
`C45` 0.1 мкФ, `C46` 10 мкФ, `R19` **2R2** в цепи обратной связи, выходы
`LCD_A`/`LCD_K` на разъём. Ровно то, что CHIPS.md §4 разобрал и отклонил: у них
последовательная цепочка светодиодов, у нас три параллельных с `VF` 3.2 В.

## 5. Расхождения

**1. Три разных числа для тока подсветки.** Спека §5.2 нормирует равномерность
при `IF` = 90 мА; лист применения (§2.5) пишет «正极A_3.3V, **75mA**»; CHIPS.md
§4 взял 90 мА и поделил на три строки по 30. Вывод: **75 мА — рабочая точка,
90 мА — условие измерения равномерности**, это не одно и то же. Расчёт ведём по
75 мА, запас по разъёму и ключу считаем по 90.

**2. Два разных потолка `VF`.** Спека §6.0 (вывод 33): «3.0V-3.4V
Typical:3.2V». Спека §5.2: `VF` типовое 3.2, **максимум 3.5**. Числа
противоречат друг другу в пределах одного документа. Разбор — §7.6.

**3. Спека — на TN-версию, панель у нас IPS.** Имя файла и §1.0 говорят о TN,
а рядом в той же папке лежит `BOE3.5IPS-ILI9488.TXT` с инициализацией, где
`0x3A` = `0x55` (RGB565) и `0x21` (инверсия для IPS). Интерфейс, распиновка и
страпы у обеих версий одинаковы — это свойство ILI9488, а не стекла; отличаются
гамма, `VCOM` и, возможно, подсветка. Вывод: **на схему это не влияет**, на
инициализацию и на §7.6 — влияет.

**4. Рекомендованный поставщиком ключ подсветки не проходит по расчёту.**
`Q1` S8050 с `R1` 10k в базе: `IB` = (3.3 − 0.7)/10k = 0.26 мА, для 75 мА нужен
`hFE` ≥ 288, а у S8050 он нормируется от 85. При `hFE` = 100 через подсветку
пойдёт 26 мА вместо 75, то есть треть яркости. Ставим **MOSFET** — у него ток
затвора нулевой и вопроса не возникает. Схему поставщика повторяем в части
подтяжки, а не в части прибора.

**5. Обе референсные платы ставят повышающий драйвер, мы не ставим.** MangoPi
`MT9284-28J`, Xassette `SY7201ABC`. Но у обеих панель с последовательной
цепочкой светодиодов, где `VF` выше питания и без буста никак. У нашей панели
три параллельные строки по 3.2 В, и **поставщик той же панели прямо пишет
«正极A_3.3V»**. Расхождение мнимое: платы решают другую задачу.

**6. Ни один 40-выводной шлейф с референсов нам не подходит.** Разъём выглядит
одинаково (40 контактов, 0.5 мм), распиновка не совпадает ни с MangoPi, ни с
Xassette. Единственный источник распиновки — §2.3. Это стоит держать в голове
при разводке: скопировать чужой футпринт можно, чужие цепи — нет.

**7. Последовательные резисторы: MangoPi ставит один, спека не требует ни
одного.** Берём практику MangoPi — 33 Ω на самый быстрый сигнал (§6.4).

**8. BOARD-draft.md устарел в двух местах.** Он числит `PD0` и `PD9` в
свободных (§5 «Свободно») — после этого блока свободных выводов в банке PD не
остаётся вовсе. И он оценивает цену второго интерфейса в «2 пина», не считая
линию `SDA` (§6.3).

## 6. Что рисуем

### 6.1 Общая раскладка шлейфа

| FFC | Панель | Режим i8080 | Режим SPI | Куда идёт |
|---|---|---|---|---|
| 1…4 | `XL` `YU` `XR` `YD` | — | — | не подключены (§2.5 п. 4) |
| 5, 16, 37 | `GND` | | | земля |
| 6, 7 | `IOVCC` | | | `+3V3`, развязка `C601`/`C602` |
| 8 | `FMARK` | — | — | не подключён (§2.3, §7.3) |
| 9 | `CS` | `CS` ← `PD21` | `CS` ← `PD21` | метка `LCD-CS` + `R607` 10k вверх |
| 10 | `RS` / `SCL` | `RS` ← `PD19` через `R604` | `SCL` ← `PC2` через `R605` | два источника, §6.3 |
| 11 | `WR` / `A0` | `WR` ← `PD18` через `R603` | `D/CX` ← `PD18` (GPIO) | метка `LCD-WR` |
| 12 | `RD` | `RD` ← `PD20` | «на `IOVCC`» | метка `LCD-RD` + `R606` 10k вверх |
| 13 | `SDA` | «на GND» | `SDA` ← `PC4` | метка `SPI0-MOSI`, §6.3 |
| 14 | `SDO` | — | — | не подключён (§2.3) |
| 15 | `RESET` | ← `PD9` | ← `PD9` | метка `LCD-RST` + `R608` 10k **вниз** |
| 17…32 | `DB0`…`DB15` | `PD1`…`PD8`, `PD10`…`PD17` | софт держит 0 | метки `LCD-DB0`…`LCD-DB15` |
| 33 | `LED-A` | | | `+3V3` через `R601` 0 Ω |
| 34…36 | `LED-K1/2/3` | | | сток `Q601` |
| 38, 40 | `IM0`, `IM2` | 0 | 1 | метка `LCD-IM` + `R609` 10k **вверх** |
| 39 | `IM1` | 1 | 1 | `+3V3` напрямую (§2.4) |

Банк PD расходуется **полностью, все 23 вывода**: 16 на данные, 4 на
управление, `PD0` — `IM_SEL`, `PD9` — `RESET` панели, `PD22` — ШИМ подсветки.
Свободных выводов в банке не остаётся.

### 6.2 Питание панели

`VCC` (6) и `IOVCC` (7) — обе на `+3V3`. Развязка у разъёма: `C601` 0.1 мкФ +
`C602` 10 мкФ, как у MangoPi (§3.3). Лист применения обходится одним 1.0 мкФ
(§2.5) — берём вариант MangoPi, он щедрее и это живая плата.

Потребление блока по `+3V3`: 5 мА панель (§2.2) + 75 мА подсветка (§2.5) =
**80 мА**. Бюджет рельсы в 02-power.md §7 — 0.5 А худшего случая при 2 А от
`SY8089`; запас есть.

Предел `VDD` панели 3.6 В (§2.2), рельса даёт 3.32 В номинала (02-power.md §3,
делитель `R8`/`R9`) — запас 0.28 В.

### 6.3 Два интерфейса на одном шлейфе

Конфликтует **ровно один вывод шлейфа — 10**. В 8080 это `RS`, который обязан
идти с `LCD0-DE` (§2.7: `A1` формирует железо TCON). В SPI это `SCL`, который
обязан идти с аппаратного `SPI0-CLK`, иначе кадр не вытолкнуть.

Решение: **два вывода F133 на одну линию**, каждый через свой резистор,
неактивный переводится в `IO Disable` (§2.9).

| Ветка | Вывод F133 | Резистор |
|---|---|---|
| 8080 | `PD19` (76) `LCD0-DE` | `R604` 33 Ω |
| SPI | `PC2` (19) `SPI0-CLK` | `R605` 33 Ω |

Остальные линии второго режима **нового вывода F133 не стоят**:

- вывод 11: в 8080 это `WR` с `LCD0-CLK`, в SPI — `D/CX`, обычный уровень;
  один и тот же `PD18`, только другой мукс;
- вывод 9 `CS`: в 8080 его дёргает TCON с `LCD0-VSYNC`, в SPI — тот же `PD21`
  как GPIO;
- вывод 13 `SDA`: в 8080 не используется, в SPI — `PC4` (`SPI0-MOSI`), уже
  занятый флешкой.

**Плата за это:** `PC2` и `PC4` — шина SPI0, на ней же сидит `W25Q32`. Пока
линия 10 шлейфа висит на `PC2`, **в режиме 8080 шиной SPI0 пользоваться
нельзя**: любой такт флешки поедет в `RS` панели. Это терпимо (прошивку
вычитывает BROM до инициализации дисплея, сейвы идут на microSD), а если
понадобится — **`R605` и есть точка размыкания**: снял резистор, SPI0 свободен,
SPI-режим дисплея потерян. Ничего больше выпаивать не нужно.

### 6.4 Последовательные резисторы

`R603` 33 Ω на вывод 11 (`WR`) — по образцу `R59` MangoPi (§3.2), которая стоит
на том же выводе F133 `PD18` и ни на каком другом. `WR` — самый быстрый сигнал
шины: он строб, по нему защёлкивается каждое слово.

`R604`, `R605` 33 Ω — см. §6.3.

На `DB0`…`DB15` резисторов **нет**: MangoPi на данных не ставит ни одного, а
шестнадцать лишних деталей на плате, которая травится дома, — это шестнадцать
шансов на непропай. Риск записан в §7.7.

Потолок сверху задаёт даташит: §18.1 «Maximum layout resistance» разрешает до
**100 Ω** на любой из этих выводов (§2.4.3). 33 Ω — треть предела, то есть
номинал можно и увеличить, если на живой плате понадобится сильнее гасить
фронты.

### 6.5 Резисторы, задающие уровень

Все четыре нужны потому, что после сброса выводы PD в третьем состоянии
(§2.9), а панель должна вести себя предсказуемо ещё до первой строчки нашего
кода.

| | Куда | Зачем |
|---|---|---|
| `R606` 10k вверх | вывод 12 `RD` | спека прямо требует «Fix to IOVCC level when not in use» (§2.3), и лист применения ставит `RD` на `VCC` в SPI-режиме (§2.5) |
| `R607` 10k вверх | вывод 9 `CS` | пока BROM читает флешку по SPI0, такт идёт и в линию 10 шлейфа (§6.3); панель обязана быть невыбранной |
| `R608` 10k **вниз** | вывод 15 `RESET` | держит панель в сбросе от подачи питания до момента, когда софт возьмёт `PD9`. Без него панель выходит из сброса в неизвестный момент и с неизвестными страпами |
| `R609` 10k вверх | выводы 38/40 `IM_SEL` | задаёт режим **до** снятия сброса. Вверх = `IM0`=`IM2`=1 = 4-line SPI — режим, в котором работает текущий код (ТЗ, требование 4) |

Порядок для софта отсюда получается жёсткий: **сначала выставить `IM_SEL`,
потом отпустить `RESET`.** Панель читает страпы при выходе из сброса.

`IM1` (39) — на `+3V3` напрямую: он равен 1 в обоих наших режимах (§2.4), и
посадочное место резистора тут ничего не даёт.

### 6.6 Подсветка

```
+3V3 ──[R601 0 Ω]──► FFC 33 (LED-A)

                                    FFC 34,35,36 (LED-K1..K3)
                                              │
PD22 (52, PWM7) ──┬──► затвор Q601 (N-MOSFET) ┘
                  │          исток ──► GND
             [R602 10k]
                  │
                +3V3
```

- `R601` **0 Ω** — посадочное место балласта. Поставщик ставит ровно 0 Ω (`R7`,
  §2.5), стенд работает так же. Номинал можно подобрать потом, не трогая плату.
- `Q601` — N-канальный MOSFET, катоды на сток, исток на землю. Не биполярный —
  причина в §5 п. 4.
- `R602` 10k **вверх** — подсветка по умолчанию включена. Так делают **оба**
  источника: лист применения (`R2` 10k на `VCC3.3V`, §2.5) и MangoPi (`R36`
  5.1k на `VCC3V3`, §3.3). Плата за это записана в §7.8.

Ток 75 мА на три катода — по 25 мА на контакт разъёма при допустимых 0.5 А
(даташит FH12, Product Specifications). Анод несёт все 75 мА на одном контакте
— тоже с шестикратным запасом.

### 6.7 Имена цепей

Метки называются по функции **вывода панели**, а не по муксу F133: блок про
дисплей, и читать схему проще так. Соответствие мукса — таблица §2.8.

`LCD-DB0`…`LCD-DB15`, `LCD-CS`, `LCD-RS`, `LCD-WR`, `LCD-RD`, `LCD-RST`,
`LCD-IM`, `LCD-BL`, плюс общие с блоком 4 `SPI0-CLK` и `SPI0-MOSI`.

Метки у ног F133 ставит **блок 8** — правило из 07-clock-reset.md §6.4 и
08-decoupling.md. Значит таблицу `PINS` в `tools/block08_decoupling.py` надо
дополнить 23 выводами банка PD со ссылкой на этот документ; `PC2` и `PC4`
принадлежат блоку 4 и появятся вместе с ним.

### 6.8 Состав блока

| Поз. | Что | Номинал | Откуда |
|---|---|---|---|
| `J601` | FFC-40 0.5 мм | Hirose FH12, `C202114` | §2.3 |
| `C601` | конденсатор | 0.1 мкФ | §6.2 |
| `C602` | конденсатор | 10 мкФ | §6.2 |
| `Q601` | `AO3400A` SOT-23 | N-MOSFET, логический уровень | §6.6, §7.1 |
| `R601` | резистор | 0 Ω | §6.6 |
| `R602` | резистор | 10k | §6.6 |
| `R603` | резистор | 33 Ω | §6.4 |
| `R604` | резистор | 33 Ω | §6.3 |
| `R605` | резистор | 33 Ω | §6.3 |
| `R606` | резистор | 10k | §6.5 |
| `R607` | резистор | 10k | §6.5 |
| `R608` | резистор | 10k | §6.5 |
| `R609` | резистор | 10k | §6.5 |

Итого 13 деталей.

### 6.9 Что схема требует от софта

Блок дёшев по железу ровно потому, что часть работы перенесена в прошивку. Всё
это следствия §2, а не пожелания:

1. **Сначала `IM_SEL`, потом `RESET`.** Панель читает страпы при выходе из
   сброса (§2.4). По умолчанию `R609` держит SPI; для 8080 прижать `PD0` в 0 и
   только после этого отпускать `PD9`.
2. **`DB0`…`DB15` выставить в 0 до снятия сброса** — и в SPI-режиме держать
   так постоянно (§2.4.1, §7.5).
3. **Импульс сброса не короче 10 мкс, после отпускания ждать до 120 мс**
   (§2.4.4).
4. **`DCLK` дисплейного контроллера не выше 25 МГц** (§2.4.2).
5. **Неактивную ветку вывода 10 переводить в `IO Disable` (`1111`), а не в
   `Input`** (§2.9, §7.4).
6. **В режиме 8080 не трогать SPI0** — ни флешку, ни что-либо ещё на этой шине
   (§6.3).

## 7. Допущения и открытые вопросы

### 7.1 MOSFET подсветки — `AO3400A`, вопрос закрыт

Требования были: N-канальный, **логический уровень** (`VGS(th)` ≤ 1.5 В,
полностью открыт при `VGS` = 2.5 В — затвор качается от 3.3 В), `ID` ≥ 0.5 А
с запасом на 75 мА, `RDS(on)` ≤ 100 мОм, корпус SOT-23. Падение на ключе прямо
съедает запас на светодиодах (§7.6), поэтому `RDS(on)` тут не формальность.

Взят **`AO3400A`**, даташит Alpha & Omega лежит в `docs/chips/AO3400A.pdf`:

| Требование | `AO3400A` | Откуда |
|---|---|---|
| `VGS(th)` ≤ 1.5 В | 0.65 / 1.05 / **1.45** В (min/typ/max) | Electrical Characteristics |
| открыт при `VGS` = 2.5 В | `RDS(on)` < **48 мΩ** при 2.5 В — параметр нормирован | Product Summary |
| `ID` ≥ 0.5 А | **5.7 А** | Product Summary |
| `RDS(on)` ≤ 100 мΩ | 26.5 мΩ при 10 В, 32 при 4.5, 48 при 2.5 | Product Summary |
| корпус SOT-23 | SOT-23, распиновка G-S-D | совпадает с символом `Q_NMOS_GSD` |

Падение на ключе в худшем случае: 48 мΩ × 75 мА = **3.6 мВ** вместо
заложенных 7.5. Запас по `VDS` (30 В против 3.3) и по току (5.7 А против
0.075) избыточен, но деталь массовая и дешевле специально подобранной.

Футпринт — штатный `Package_TO_SOT_SMD:SOT-23`.

### 7.2 Вариант разъёма — `FH12-40S-0.5SH`, нижний контакт, вопрос закрыт

FH12 бывает top contact, bottom contact и вертикальный (даташит, §Features
п. 4). Какой нужен — зависит от того, какой стороной шлейф выходит из панели и
как панель ляжет в корпус. Механика теперь определена: **панель на лицевой
стороне платы, вся электроника на изнанке, разъём под панелью на лице,
прорези в плате нет** — шлейф заворачивается на изнанку самой панели.

**Какой стороной шлейф несёт контакты.** Спека панели, стр. 4, два вида одного
и того же хвоста:

| Вид | Что нарисовано на хвосте | Нумерация |
|---|---|---|
| спереди (со стороны экрана) | **сорок контактных пальцев**, шаг 0.5, размах 19.5 | `1` слева, `40` справа |
| сзади (`COMPONENT AREA`) | **`PI补强`** — полиимидная подложка, 4 мм | `40` слева, `1` справа |

Нумерация в двух видах зеркальна — значит это действительно противоположные
стороны одного хвоста. Подложка у FFC всегда клеится **напротив** контактов,
она подпирает их под прижим разъёма. Отсюда: **контакты на лицевой стороне
шлейфа, той же, что и экран.**

**Цепочка поворотов.** Считаем от лица платы вверх:

1. Панель лежит экраном вверх → хвост выходит из её торца контактами **вверх**.
2. Хвост заворачивается на 180° через торец панели себе на изнанку (спека,
   вид сзади: сгиб `1.0 MAX`, вылет по изнанке `43.72±0.5`) → контакты
   смотрят **вниз**, на плату.
3. От изнанки панели шлейф опускается на 1.4 мм в разъём — это плавный
   изгиб без переворота, контакты остаются **внизу**.

**Что требует разъём.** Даташит FH12, «Series Configuration»: у семейства
`FH12-**S-0.5SH` подпись «FPC/FFC conductive surface **(bottom side)**», а
верхний контакт — это отдельный шифр `FH12**A**-**S-0.5SH` (стр. 7). Расшифровка
партномера там же: «A : Top contact type». Нижний контакт хочет проводники
вниз.

**Совпало.** Шлейф приходит контактами вниз, разъём ждёт контакты внизу.
`FH12-40S-0.5SH`, который уже стоит в схеме, менять не надо.

**Толщина заодно сошлась.** Кончик хвоста `0.30±0.03` (спека, разрез), а FH12
рассчитан на `t = 0.3±0.05` (даташит, «Adaptive FPC/FFC contact
specifications»). Вариант `FH12F` — это для 0.18, он нам не нужен.

**Что из этого следует для разводки.** Панель ландшафтная, хвост выходит из её
**левого** торца и заворачивается вправо, под панель. Сгиб — вокруг
вертикальной оси, а такой поворот зеркалит только «влево-вправо» и не трогает
«вверх-вниз». Значит, если смотреть на лицо платы, **контакт 1 оказывается со
стороны верхнего края платы, контакт 40 — со стороны нижнего**, а окно разъёма
должно смотреть **влево**.

### 7.2.1 Заворот переставляет контакты местами — свой футпринт

Два требования выше выполнить одновременно штатным футпринтом **нельзя**.
Обмер `Connector_FFC-FPC:Hirose_FH12-40S-0.5SH_1x40-1MP_P0.50mm_Horizontal`:
площадки 1…40 идут по X от −9.75 до +9.75 на `y = −1.85`, корпус по `F.Fab`
занимает `y` от −1.2 до +4.4. То есть площадки вынесены перед корпусом, там и
вход шлейфа — со стороны −Y. Отсюда таблица поворотов:

| поворот | куда смотрит окно | где вывод 1 |
|---|---|---|
| 0° | вверх | слева |
| **90°** | **влево** ✓ | **внизу** ✗ |
| 180° | вниз | справа |
| 270° | вправо | вверху |

Окно влево даёт только 90°, а там вывод 1 внизу — шлейф же приносит контакт 1
сверху. Развернуть панель хвостом вправо не помогает: зеркалятся и шлейф, и
требование к разъёму, несовпадение сохраняется. Причина общая — заворот на
180° меняет «рукость» шлейфа, а корпус на той же стороне платы отзеркалить
нечем.

Сменой производителя это тоже не лечится. В библиотеке KiCad ровно **пять**
футпринтов на 40 контактов с шагом 0.5 (`Hirose FH12`, `Amphenol F32Q` и
`F32R`, `Omron XF2M-4015-1A`, `TE 4-1734839-0`), вертикальных на 40 контактов
нет вовсе — и у всех пяти одна и та же «рукость»: рот наверх, вывод 1 слева.

**Решение.** Факт механики — контакт `N` панели ложится на контакт `41−N`
разъёма — записан **в футпринте**, а не в схеме:
`console:FH12-40S-0.5SH_ContactsReversed`, геометрия побайтово из штатного,
переставлены только номера площадок. Так `J601` вывод `N` остаётся выводом `N`
панели, и схема читается рядом со спекой панели один в один; вся кривизна
сидит в одном файле, который глазами никто не читает.

Альтернатива — перенумеровать 40 строк в схеме — отвергнута: тогда до конца
проекта пришлось бы помнить, что вывод 1 несёт сигнал сорокового.

### 7.3 `FMARK` (tearing effect) не подключён

Спека разрешает («If not used, open this pin»), лист применения делает именно
так. Но в эмуляторе с полнокадровым выводом разрыв кадра виден.

Подключить некуда: банк PD израсходован полностью (§6.1). Формально у F133 есть
`DBI-TE` на `PD13` (Function4, §2.6), но `PD13` занят под `DB11`. Остаётся
свободный банк PB — то есть провод через весь лист и через всю плату ради
сигнала, который нужен только если тиринг реально полезет.

**Решено: не подключаем** — на стенде `FMARK` тоже не подключён, и панель там
работает. Если тиринг будет мешать, это доработка платы, а не переразводка:
провод от `PB` до вывода 8 разъёма можно накинуть и постфактум.

### 7.4 Конкуренция на выводе 10 шлейфа

Если софт по ошибке включит одновременно `LCD0-DE` на `PD19` и `SPI0-CLK` на
`PC2`, встречный ток пойдёт через `R604` + `R605` = 66 Ω, то есть до 50 мА при
разнополярных уровнях. Это заметно больше 6 мА, на которые нормирован вывод
(02-power.md §2.2).

Защита — программная: `PD_CFG` после сброса и так `IO Disable` (§2.9), и
неактивную ветку нужно возвращать в `1111`, а не в `Input`.

Поднять номиналы даташит позволяет: предел 100 Ω на вывод (§2.4.3), то есть
82 Ω в каждой ветке загнали бы худший случай в 20 мА. Но 82 Ω на `SCL` в
SPI-режиме — это постоянная времени 82 Ω × ~15 пФ ≈ 1.2 нс, а сам режим и так
объявлен нерекомендованным (§2.5); платить за защиту от программной ошибки
деградацией штатного сигнала не хочется. **Принято: 33 Ω и правило для софта.**

### 7.5 В режиме SPI шину `DB0`…`DB15` держит софт, а не резисторы

Требование подтверждено на всех трёх уровнях: спека модуля «Fix to GND level
when not in use» (§2.3), лист применения сажает `17-32PIN_DB0-15` прямо на
землю (§2.5), даташит самого контроллера — «**Fix to DGND level when not in
use**» (§2.4.1). То есть это не перестраховка сборщика модуля, а требование
ILI Technology.

Шестнадцать резисторов вниз мы всё равно не ставим — вместо этого софт
переводит `PD1`…`PD17` в выход и выдаёт 0 **до** снятия `RESET` панели.

Чем рискуем: между подачей питания и этим моментом входы `DB` панели висят в
воздухе (§2.9). Панель при этом в сбросе (`R608`), но её входные буферы под
питанием, и плавающий вход КМОП-буфера — это сквозной ток. Величина в даташите
не нормирована, время — доли секунды. **Считаю допустимым, но это допущение, а
не факт**, и единственное место в блоке, где мы сознательно расходимся с прямым
требованием даташита.

### 7.6 Запаса по напряжению на подсветке практически нет

Считаем от рельсы 3.32 В (02-power.md §3):

| Случай | `VF` | Остаток на `R601` + ключ |
|---|---|---|
| типовой (§2.2) | 3.2 В | 0.12 В |
| потолок по §6.0 спеки | 3.4 В | **−0.08 В** |
| потолок по §5.2 спеки | 3.5 В | **−0.18 В** |

При `VF` выше 3.3 В подсветка от рельсы 3.3 В **не зажжётся вовсе**. Против
этого есть ровно один аргумент, зато сильный: **поставщик той же самой панели
пишет «正极A_3.3V, 75mA»** (§2.5), то есть 3.3 В — это заявленная рабочая
точка, а «максимум 3.5» относится к разбросу партии и, видимо, к другому току.
Плюс стенд с этой панелью уже светится от 3.3 В (CHIPS.md §4).

Что принимаем осознанно: ток зависит от температуры (`VF` падает ~2 мВ/°C, за
игру панель греется на 20–40°) и уходит в ноль в конце разряда, когда рельса
3.3 В садится вслед за батареей. ШИМ-регулировка при этом работает — ключ рвёт
цепь, запас напряжения ему не нужен.

Страховка на плате: `R601` — посадочное место, а не перемычка. Если на живой
плате яркости не хватит, туда встанет буст, а не новая плата.

### 7.7 Шестнадцать линий шины без резисторов и без экрана

Данных резисторов нет (§6.4), шлейф FFC не экранирован, плата двухслойная и,
возможно, травленая дома. При 480×320×16 бит и 60 кадрах это ~150 Мбит/с
суммарно по шине. Основной риск блока по ЭМС.

Обосновано тем, что MangoPi так и делает на серийной плате (§3.2). Не проверено
ничем: у MangoPi шлейф RGB с непрерывным потоком, у нас 8080 со стробом, и
характер помех другой.

### 7.8 Подсветка включена по умолчанию

`R602` тянет затвор вверх (§6.6), как оба источника. Значит подсветка горит с
момента появления `+3V3` — до старта софта, во время загрузки и, что важнее,
если консоль зависла. Это 75 мА × 3.3 В = 250 мВт из батареи.

Взамен получаем индикатор: экран засветился — рельса 3.3 В жива. На этапе 5
первого запуска это единственное, что вообще видно.

Обратный вариант (подтяжка вниз) отвергнут потому, что оба источника выбрали
вверх, а никакого своего довода в пользу «вниз», кроме этих 250 мВт, у нас нет.
**Решение подтверждено**, вопрос закрыт. Если понадобится — меняется одним
резистором, не переразводкой.

### 7.9 Тайминги сошлись, порядок инициализации — нет

Вопрос «прокачает ли TCON F133 эту панель» **закрыт расчётом** в §2.4.2:
`twc` ≥ 40 нс против нужных 108 нс на 60 кадров/с — запас 2.7 раза, потолок
`DCLK` 25 МГц против нужных ~12. User manual обещает «Supports i8080 interface,
up to 800 × 480@60fps» (§5.1 «Overview», стр. 365), и цифры это подтверждают,
а не просто повторяют.

Что осталось не проверенным — **порядок инициализации**, а не электрика:
Figure 5-13 «i8080 Mode Initial Process» (стр. 384) содержит одиннадцать шагов
и заканчивается операцией «tri start» (бит 1 в `LCD_CPU_IF_REG`), то есть режим
не сводится к «настроил регистры и забыл». Плюс поставщик пишет, что для 3.5″
последовательный интерфейс не рекомендуется (§2.5) — то есть запасной режим
заведомо медленный, и рассчитывать на него как на постоянный нельзя.

**Это задача софта, а не схемы.** На разводку не влияет.

### 7.9.1 Последовательные резисторы поставлены не в разрыв линии

`R603`…`R605` стоят последовательно в линиях, то есть их место — на пути от
вывода F133 к контакту разъёма. Сейчас они лежат в блоке слева от `J601`, а
корпус справа от него: сигнал шёл бы от чипа мимо разъёма к резистору и
обратно, крюк порядка десяти миллиметров.

Так вышло потому, что между корпусом и разъёмом зазора нет вовсе — кольцо
развязки начинается там же, где кончается `J601`. Резистор придётся ставить
выше или ниже коридора, и куда именно — станет ясно при разводке. Записано в
10-mech.md §10.1.

### 7.10 Что осталось непроверенным по источникам

Источники блока полны: даташит контроллера, спека модуля, лист применения
поставщика, даташит и user manual F133, даташит разъёма, две референсные платы.
Ни одного номинала без ссылки в блоке нет.

Не проверено ничем:

- **отсутствие резисторов на 16 линиях данных** (§7.7) — обосновано практикой
  MangoPi, но у них RGB, а у нас 8080 со стробом;
- **плавающая шина `DB` до старта софта** (§7.5) — прямое расхождение с
  даташитом, принятое сознательно;
- **`VF` подсветки на конкретной панели** (§7.6) — измеряется на живой плате,
  под это оставлено посадочное место `R601`.
